# Bloco OAuth do M4 (Conexão Mercado Livre) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Conectar a conta de vendedor do Mercado Livre ao PubliAI via OAuth 2.0 e expor `getValidAccessToken(user_id)` para que os blocos seguintes do M4 sempre tenham um access token válido.

**Architecture:** Três Edge Functions Deno (`ml-oauth-start` autenticada, `ml-oauth-callback` pública, `ml-oauth-disconnect` autenticada). O `state` (CSRF) vive no Upstash Redis com TTL. Tokens são gravados/lidos pelas RPCs de Vault já existentes do M2 (`upsert_ml_credentials`/`get_ml_tokens`); o disconnect ganha uma RPC nova `delete_ml_credentials`. O refresh é centralizado em `getValidAccessToken`, com lock distribuído `SET NX` (ADR-0012). Frontend ganha um hook de estado e troca o mock da seção ML em Configurações.

**Tech Stack:** Deno (Edge Functions), Supabase JS (service role), Upstash Redis REST, Supabase Vault, React + TanStack Query, vitest.

**Specs/ADRs:** [spec](../specs/2026-05-29-m4-oauth-ml-design.md), [ADR-0011](../../decisions/0011-redirect-uri-via-edge-function.md), [ADR-0012](../../decisions/0012-refresh-token-oauth-ml-com-lock-redis.md).

**Convenções de teste deste repo:** os testes de `supabase/functions/**/__tests__/*.test.ts` rodam no **vitest** (não `deno test`) e só conseguem importar **arquivos puros** — qualquer arquivo que importe `jsr:@supabase/...` ou use `Deno.env` no topo quebra o import no vitest. Por isso a lógica testável fica isolada em arquivos puros (`auth-url.ts`, `refresh-decisao.ts`); a orquestração com I/O (`token.ts`, Edge Functions, redis) é validada manualmente no bug bash.

---

## File structure

| Arquivo | Responsabilidade |
|---|---|
| `supabase/migrations/20260529000001_delete_ml_credentials.sql` | RPC `delete_ml_credentials` para o disconnect |
| `supabase/functions/_shared/redis/client.ts` (modificar) | +`redisSetNX` |
| `supabase/functions/_shared/ml/auth-url.ts` (criar) | `montarAuthUrl` (puro) |
| `supabase/functions/_shared/ml/refresh-decisao.ts` (criar) | `precisaRenovar` (puro) |
| `supabase/functions/_shared/ml/token.ts` (criar) | `trocarCodePorToken`, `refreshTokenML`, `getValidAccessToken` |
| `supabase/functions/_shared/ml/__tests__/auth-url.test.ts` (criar) | teste de `montarAuthUrl` |
| `supabase/functions/_shared/ml/__tests__/refresh-decisao.test.ts` (criar) | teste de `precisaRenovar` |
| `supabase/functions/ml-oauth-start/index.ts` (criar) | inicia o fluxo (state + authUrl) |
| `supabase/functions/ml-oauth-callback/index.ts` (criar) | troca code→token, upsert, redirect |
| `supabase/functions/ml-oauth-disconnect/index.ts` (criar) | apaga credencial + segredos |
| `src/lib/ml-oauth.ts` (criar) | `iniciarConexaoML`, `desconectarML` |
| `src/hooks/useMlConnection.ts` (criar) | estado conectado/nickname |
| `src/pages/Configuracoes.tsx` (modificar) | seção ML real + banner de status |

---

## Task 0: Provisionar secrets do ML no Supabase

**Files:** nenhum (setup de ambiente)

- [ ] **Step 1: Conferir os valores no `.env.local`**

Run: `grep -E '^ML_(CLIENT_ID|CLIENT_SECRET|REDIRECT_URI)=' ".env.local"`
Expected: três linhas com os valores.

- [ ] **Step 2: Provisionar como secrets das Edge Functions**

As Edge Functions leem `Deno.env.get('ML_CLIENT_ID' | 'ML_CLIENT_SECRET' | 'ML_REDIRECT_URI')`. `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` e as chaves `UPSTASH_*` já estão provisionadas (M2/M3).

Via Supabase CLI (precisa de login — pode exigir o Diego rodar com `!`):
```bash
supabase secrets set ML_CLIENT_ID=<valor> ML_CLIENT_SECRET=<valor> ML_REDIRECT_URI=https://txvncrgkoynoxwopfkbp.supabase.co/functions/v1/ml-oauth-callback --project-ref txvncrgkoynoxwopfkbp
```
Se o agente não tiver CLI logada, **pedir ao Diego** para rodar o comando acima (ou setar no Dashboard → Edge Functions → Secrets). Não prosseguir para o deploy (Task 9) sem isso.

- [ ] **Step 3: Confirmar**

Run: `supabase secrets list --project-ref txvncrgkoynoxwopfkbp` (ou conferir no Dashboard)
Expected: `ML_CLIENT_ID`, `ML_CLIENT_SECRET`, `ML_REDIRECT_URI` listados.

---

## Task 1: Migration `delete_ml_credentials` + regenerar tipos

**Files:**
- Create: `supabase/migrations/20260529000001_delete_ml_credentials.sql`
- Modify: `src/lib/database.types.ts` (via regeneração)

- [ ] **Step 1: Escrever a migration**

```sql
-- ============================================================================
-- Migration — delete_ml_credentials (suporte ao disconnect do OAuth ML)
-- Ref: spec docs/superpowers/specs/2026-05-29-m4-oauth-ml-design.md (M4 bloco OAuth)
-- Apaga os dois segredos do Vault referenciados + a linha de ml_credentials.
-- ============================================================================

create or replace function public.delete_ml_credentials(p_user_id uuid)
returns void
language plpgsql
security definer
set search_path = public, vault
as $$
declare
  v_creds public.ml_credentials%rowtype;
begin
  select * into v_creds from public.ml_credentials where user_id = p_user_id;
  if v_creds.user_id is null then
    return; -- idempotente: nada a apagar
  end if;

  delete from vault.secrets where id = v_creds.access_token_secret_id;
  delete from vault.secrets where id = v_creds.refresh_token_secret_id;
  delete from public.ml_credentials where user_id = p_user_id;
end;
$$;

revoke execute on function public.delete_ml_credentials(uuid) from public, anon, authenticated;
```

- [ ] **Step 2: Aplicar a migration**

Aplicar via MCP `apply_migration` (project_id `txvncrgkoynoxwopfkbp`, name `delete_ml_credentials`, query = conteúdo do arquivo).
Expected: sucesso, sem erro.

- [ ] **Step 3: Regenerar os tipos TypeScript**

Via MCP `generate_typescript_types` (project_id `txvncrgkoynoxwopfkbp`) e salvar em `src/lib/database.types.ts`.
Expected: o bloco `Functions` passa a conter `delete_ml_credentials: { Args: { p_user_id: string }, Returns: undefined }`.

- [ ] **Step 4: Verificar o build de tipos**

Run: `cd "/Users/diego/Desktop/IA/Anuncios MktPlace" && pnpm exec tsc -b --noEmit`
Expected: sem erros.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260529000001_delete_ml_credentials.sql src/lib/database.types.ts
git commit -m "feat(m4): rpc delete_ml_credentials para o disconnect do OAuth ML"
```

---

## Task 2: Helper `redisSetNX`

**Files:**
- Modify: `supabase/functions/_shared/redis/client.ts`

> Sem teste unitário (I/O com Upstash; padrão do repo é testar só funções puras). Validado no bug bash via o lock real.

- [ ] **Step 1: Adicionar a função ao final do arquivo**

```typescript
export async function redisSetNX(
  chave: string,
  valor: string,
  ttlSegundos: number,
): Promise<boolean> {
  const r = await call<string>(['SET', chave, valor, 'NX', 'EX', ttlSegundos]);
  return r === 'OK';
}
```

(O Upstash REST devolve `"OK"` quando o `SET NX` grava e `null` quando a chave já existe.)

- [ ] **Step 2: Conferir que nada quebrou no type check**

Run: `cd "/Users/diego/Desktop/IA/Anuncios MktPlace" && pnpm exec tsc -b --noEmit`
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add supabase/functions/_shared/redis/client.ts
git commit -m "feat(m4): redisSetNX para lock distribuído do refresh ML"
```

---

## Task 3: `montarAuthUrl` (função pura, TDD)

**Files:**
- Create: `supabase/functions/_shared/ml/auth-url.ts`
- Test: `supabase/functions/_shared/ml/__tests__/auth-url.test.ts`

- [ ] **Step 1: Escrever o teste que falha**

```typescript
import { describe, it, expect } from 'vitest';
import { montarAuthUrl } from '../auth-url';

describe('montarAuthUrl', () => {
  it('monta a URL de autorização do ML Brasil com os params certos', () => {
    const url = montarAuthUrl('abc-123', 'CLIENT_X', 'https://x.supabase.co/functions/v1/ml-oauth-callback');
    const u = new URL(url);
    expect(u.origin + u.pathname).toBe('https://auth.mercadolibre.com.br/authorization');
    expect(u.searchParams.get('response_type')).toBe('code');
    expect(u.searchParams.get('client_id')).toBe('CLIENT_X');
    expect(u.searchParams.get('redirect_uri')).toBe('https://x.supabase.co/functions/v1/ml-oauth-callback');
    expect(u.searchParams.get('state')).toBe('abc-123');
  });
});
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `cd "/Users/diego/Desktop/IA/Anuncios MktPlace" && pnpm exec vitest run supabase/functions/_shared/ml/__tests__/auth-url.test.ts`
Expected: FAIL (`Cannot find module '../auth-url'`).

- [ ] **Step 3: Implementar**

```typescript
export function montarAuthUrl(
  state: string,
  clientId: string,
  redirectUri: string,
): string {
  const params = new URLSearchParams({
    response_type: 'code',
    client_id: clientId,
    redirect_uri: redirectUri,
    state,
  });
  return `https://auth.mercadolibre.com.br/authorization?${params.toString()}`;
}
```

- [ ] **Step 4: Rodar o teste e ver passar**

Run: `cd "/Users/diego/Desktop/IA/Anuncios MktPlace" && pnpm exec vitest run supabase/functions/_shared/ml/__tests__/auth-url.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add supabase/functions/_shared/ml/auth-url.ts supabase/functions/_shared/ml/__tests__/auth-url.test.ts
git commit -m "feat(m4): montarAuthUrl (URL de autorização OAuth ML)"
```

---

## Task 4: `precisaRenovar` (função pura, TDD)

**Files:**
- Create: `supabase/functions/_shared/ml/refresh-decisao.ts`
- Test: `supabase/functions/_shared/ml/__tests__/refresh-decisao.test.ts`

- [ ] **Step 1: Escrever o teste que falha**

```typescript
import { describe, it, expect } from 'vitest';
import { precisaRenovar } from '../refresh-decisao';

const BUFFER = 5 * 60 * 1000; // 5 min

describe('precisaRenovar', () => {
  it('NÃO renova quando falta bem mais que o buffer', () => {
    const agora = 1_000_000;
    const expira = agora + 60 * 60 * 1000; // +1h
    expect(precisaRenovar(expira, agora, BUFFER)).toBe(false);
  });

  it('renova quando falta menos que o buffer', () => {
    const agora = 1_000_000;
    const expira = agora + 2 * 60 * 1000; // +2 min
    expect(precisaRenovar(expira, agora, BUFFER)).toBe(true);
  });

  it('renova quando já expirou', () => {
    const agora = 1_000_000;
    const expira = agora - 1000; // já passou
    expect(precisaRenovar(expira, agora, BUFFER)).toBe(true);
  });

  it('renova exatamente no limite do buffer', () => {
    const agora = 1_000_000;
    const expira = agora + BUFFER;
    expect(precisaRenovar(expira, agora, BUFFER)).toBe(true);
  });
});
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `cd "/Users/diego/Desktop/IA/Anuncios MktPlace" && pnpm exec vitest run supabase/functions/_shared/ml/__tests__/refresh-decisao.test.ts`
Expected: FAIL (`Cannot find module '../refresh-decisao'`).

- [ ] **Step 3: Implementar**

```typescript
/** true se o token expira dentro de `bufferMs` (ou já expirou). */
export function precisaRenovar(
  expiresAtMs: number,
  agoraMs: number,
  bufferMs: number,
): boolean {
  return expiresAtMs - agoraMs <= bufferMs;
}
```

- [ ] **Step 4: Rodar o teste e ver passar**

Run: `cd "/Users/diego/Desktop/IA/Anuncios MktPlace" && pnpm exec vitest run supabase/functions/_shared/ml/__tests__/refresh-decisao.test.ts`
Expected: PASS (4 testes).

- [ ] **Step 5: Commit**

```bash
git add supabase/functions/_shared/ml/refresh-decisao.ts supabase/functions/_shared/ml/__tests__/refresh-decisao.test.ts
git commit -m "feat(m4): precisaRenovar (decisão de refresh com buffer)"
```

---

## Task 5: `token.ts` — troca, refresh e `getValidAccessToken`

**Files:**
- Create: `supabase/functions/_shared/ml/token.ts`

> Sem teste unitário (importa `adminClient`/`Deno.env` → não roda no vitest). A decisão pura já é coberta pela Task 4; a orquestração é validada no bug bash (Task 14).

- [ ] **Step 1: Implementar o arquivo**

```typescript
import { adminClient } from '../supabase.ts';
import { redisSetNX, redisDel } from '../redis/client.ts';
import { precisaRenovar } from './refresh-decisao.ts';

const TOKEN_URL = 'https://api.mercadolibre.com/oauth/token';
const BUFFER_MS = 5 * 60 * 1000;
const LOCK_TTL_S = 30;
const REFRESH_WAIT_TRIES = 10;
const REFRESH_WAIT_MS = 300;

export interface TokenML {
  access_token: string;
  refresh_token: string;
  expires_in: number;
  scope?: string;
  user_id: number;
}

const sleep = (ms: number) => new Promise((r) => setTimeout(r, ms));

async function postToken(params: Record<string, string>): Promise<TokenML> {
  const resp = await fetch(TOKEN_URL, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/x-www-form-urlencoded',
      'Accept': 'application/json',
    },
    body: new URLSearchParams({
      client_id: Deno.env.get('ML_CLIENT_ID')!,
      client_secret: Deno.env.get('ML_CLIENT_SECRET')!,
      ...params,
    }),
  });
  if (!resp.ok) {
    throw new Error(`ML /oauth/token ${resp.status}: ${await resp.text()}`);
  }
  return resp.json() as Promise<TokenML>;
}

/** Troca o `code` do callback por tokens. */
export function trocarCodePorToken(code: string): Promise<TokenML> {
  return postToken({
    grant_type: 'authorization_code',
    code,
    redirect_uri: Deno.env.get('ML_REDIRECT_URI')!,
  });
}

/** Renova usando o refresh_token (rotativo: a resposta traz um novo refresh). */
export function refreshTokenML(refreshToken: string): Promise<TokenML> {
  return postToken({ grant_type: 'refresh_token', refresh_token: refreshToken });
}

async function lerTokens(userId: string) {
  const { data, error } = await adminClient().rpc('get_ml_tokens', { p_user_id: userId });
  if (error) throw new Error(`get_ml_tokens: ${error.message}`);
  const row = (data as Array<{ access_token: string; refresh_token: string; expires_at: string }>)?.[0];
  if (!row) throw new Error(`Sem credenciais ML para o usuário ${userId}`);
  return row;
}

async function gravarRotacao(userId: string, tok: TokenML) {
  // Mantém ml_user_id/nickname existentes (refresh não retorna nickname).
  const { data: meta } = await adminClient()
    .from('ml_credentials')
    .select('ml_user_id, ml_nickname')
    .eq('user_id', userId)
    .maybeSingle();
  const expiresAt = new Date(Date.now() + tok.expires_in * 1000).toISOString();
  const { error } = await adminClient().rpc('upsert_ml_credentials', {
    p_user_id: userId,
    p_ml_user_id: meta?.ml_user_id ?? String(tok.user_id),
    p_ml_nickname: meta?.ml_nickname ?? null,
    p_access_token: tok.access_token,
    p_refresh_token: tok.refresh_token,
    p_scope: tok.scope ?? null,
    p_expires_at: expiresAt,
  });
  if (error) throw new Error(`upsert_ml_credentials: ${error.message}`);
}

/**
 * Retorna um access token válido para o usuário, renovando proativamente
 * (buffer de 5 min) com lock distribuído (ADR-0012) para não quebrar o
 * refresh_token rotativo quando várias famílias rodam em paralelo.
 */
export async function getValidAccessToken(userId: string): Promise<string> {
  const row = await lerTokens(userId);
  if (!precisaRenovar(Date.parse(row.expires_at), Date.now(), BUFFER_MS)) {
    return row.access_token;
  }

  const lockKey = `lock:ml:refresh:${userId}`;
  const pegouLock = await redisSetNX(lockKey, '1', LOCK_TTL_S);

  if (pegouLock) {
    try {
      const tok = await refreshTokenML(row.refresh_token);
      await gravarRotacao(userId, tok);
      return tok.access_token;
    } finally {
      await redisDel(lockKey);
    }
  }

  // Outro processo está renovando: espera o expires_at avançar e relê.
  for (let i = 0; i < REFRESH_WAIT_TRIES; i++) {
    await sleep(REFRESH_WAIT_MS);
    const r2 = await lerTokens(userId);
    if (!precisaRenovar(Date.parse(r2.expires_at), Date.now(), BUFFER_MS)) {
      return r2.access_token;
    }
  }
  throw new Error('Timeout aguardando refresh de token ML (lock concorrente)');
}
```

- [ ] **Step 2: Verificar type check do frontend (não deve ser afetado, mas garante que nada do `src` importa errado)**

Run: `cd "/Users/diego/Desktop/IA/Anuncios MktPlace" && pnpm exec tsc -b --noEmit`
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add supabase/functions/_shared/ml/token.ts
git commit -m "feat(m4): getValidAccessToken com refresh proativo + lock (ADR-0012)"
```

---

## Task 6: Edge Function `ml-oauth-start`

**Files:**
- Create: `supabase/functions/ml-oauth-start/index.ts`

- [ ] **Step 1: Implementar**

```typescript
import { corsHeaders, handleOptions } from '../_shared/cors.ts';
import { userClient } from '../_shared/supabase.ts';
import { redisSet } from '../_shared/redis/client.ts';
import { montarAuthUrl } from '../_shared/ml/auth-url.ts';

const STATE_TTL_S = 600; // 10 min

Deno.serve(async (req) => {
  if (req.method === 'OPTIONS') return handleOptions();
  if (req.method !== 'POST') {
    return new Response('Method not allowed', { status: 405, headers: corsHeaders });
  }

  const auth = req.headers.get('authorization');
  if (!auth?.startsWith('Bearer ')) {
    return new Response('Missing auth', { status: 401, headers: corsHeaders });
  }
  const { data: { user } } = await userClient(auth.slice(7)).auth.getUser();
  if (!user) return new Response('Invalid token', { status: 401, headers: corsHeaders });

  const state = crypto.randomUUID();
  await redisSet(`oauth:ml:state:${state}`, user.id, STATE_TTL_S);

  const authUrl = montarAuthUrl(
    state,
    Deno.env.get('ML_CLIENT_ID')!,
    Deno.env.get('ML_REDIRECT_URI')!,
  );

  return new Response(JSON.stringify({ authUrl }), {
    status: 200,
    headers: { ...corsHeaders, 'Content-Type': 'application/json' },
  });
});
```

- [ ] **Step 2: Commit**

```bash
git add supabase/functions/ml-oauth-start/index.ts
git commit -m "feat(m4): edge ml-oauth-start (gera state + authUrl)"
```

---

## Task 7: Edge Function `ml-oauth-callback`

**Files:**
- Create: `supabase/functions/ml-oauth-callback/index.ts`

- [ ] **Step 1: Implementar**

```typescript
import { adminClient } from '../_shared/supabase.ts';
import { redisGet, redisDel } from '../_shared/redis/client.ts';
import { trocarCodePorToken } from '../_shared/ml/token.ts';

const FRONTEND = 'https://ean2marketplace-frontend.onrender.com/#/configuracoes';

function redirect(query: string): Response {
  return new Response(null, { status: 302, headers: { Location: `${FRONTEND}?${query}` } });
}

async function buscarNickname(mlUserId: number, accessToken: string): Promise<string | null> {
  try {
    const r = await fetch(`https://api.mercadolibre.com/users/${mlUserId}`, {
      headers: { Authorization: `Bearer ${accessToken}` },
    });
    if (!r.ok) return null;
    const j = await r.json() as { nickname?: string };
    return j.nickname ?? null;
  } catch {
    return null; // nickname é best-effort
  }
}

Deno.serve(async (req) => {
  const url = new URL(req.url);
  const code = url.searchParams.get('code');
  const state = url.searchParams.get('state');

  if (!code || !state) return redirect('ml_erro=state');

  const userId = await redisGet(`oauth:ml:state:${state}`);
  if (!userId) return redirect('ml_erro=state');
  await redisDel(`oauth:ml:state:${state}`); // uso único

  try {
    const tok = await trocarCodePorToken(code);
    const nickname = await buscarNickname(tok.user_id, tok.access_token);
    const expiresAt = new Date(Date.now() + tok.expires_in * 1000).toISOString();

    const { error } = await adminClient().rpc('upsert_ml_credentials', {
      p_user_id: userId,
      p_ml_user_id: String(tok.user_id),
      p_ml_nickname: nickname,
      p_access_token: tok.access_token,
      p_refresh_token: tok.refresh_token,
      p_scope: tok.scope ?? null,
      p_expires_at: expiresAt,
    });
    if (error) throw new Error(error.message);

    return redirect('ml_conectado=true');
  } catch (e) {
    console.error('ml-oauth-callback erro:', e instanceof Error ? e.message : String(e));
    return redirect('ml_erro=token');
  }
});
```

- [ ] **Step 2: Commit**

```bash
git add supabase/functions/ml-oauth-callback/index.ts
git commit -m "feat(m4): edge ml-oauth-callback (troca code->token, upsert, redirect)"
```

---

## Task 8: Edge Function `ml-oauth-disconnect`

**Files:**
- Create: `supabase/functions/ml-oauth-disconnect/index.ts`

- [ ] **Step 1: Implementar**

```typescript
import { corsHeaders, handleOptions } from '../_shared/cors.ts';
import { userClient, adminClient } from '../_shared/supabase.ts';

Deno.serve(async (req) => {
  if (req.method === 'OPTIONS') return handleOptions();
  if (req.method !== 'POST') {
    return new Response('Method not allowed', { status: 405, headers: corsHeaders });
  }

  const auth = req.headers.get('authorization');
  if (!auth?.startsWith('Bearer ')) {
    return new Response('Missing auth', { status: 401, headers: corsHeaders });
  }
  const { data: { user } } = await userClient(auth.slice(7)).auth.getUser();
  if (!user) return new Response('Invalid token', { status: 401, headers: corsHeaders });

  const { error } = await adminClient().rpc('delete_ml_credentials', { p_user_id: user.id });
  if (error) {
    return new Response(`delete_ml_credentials: ${error.message}`, { status: 500, headers: corsHeaders });
  }
  return new Response('OK', { status: 200, headers: corsHeaders });
});
```

- [ ] **Step 2: Commit**

```bash
git add supabase/functions/ml-oauth-disconnect/index.ts
git commit -m "feat(m4): edge ml-oauth-disconnect"
```

---

## Task 9: Deploy das três Edge Functions

**Files:** nenhum (deploy via MCP)

> Pré-requisito: Task 0 concluída (secrets `ML_*` provisionados).

- [ ] **Step 1: Deploy via MCP `deploy_edge_function`**

Para cada função, project_id `txvncrgkoynoxwopfkbp`, **`verify_jwt: false`** (todas fazem auth manual; o callback é chamado pelo redirect público do ML, sem JWT):
- `ml-oauth-start` (entrypoint `index.ts`)
- `ml-oauth-callback` (entrypoint `index.ts`)
- `ml-oauth-disconnect` (entrypoint `index.ts`)

Incluir os arquivos `_shared/` referenciados (o MCP empacota o diretório da função + imports relativos; se o MCP exigir lista explícita de arquivos, incluir `_shared/cors.ts`, `_shared/supabase.ts`, `_shared/redis/client.ts`, `_shared/ml/auth-url.ts`, `_shared/ml/refresh-decisao.ts`, `_shared/ml/token.ts`).

- [ ] **Step 2: Smoke test do `ml-oauth-start` (precisa de JWT de usuário)**

Com a sessão do app aberta, obter o access token (DevTools) e:
```bash
curl -i -X POST "https://txvncrgkoynoxwopfkbp.supabase.co/functions/v1/ml-oauth-start" \
  -H "Authorization: Bearer <ACCESS_TOKEN_DO_USUARIO>"
```
Expected: HTTP 200 com `{"authUrl":"https://auth.mercadolibre.com.br/authorization?..."}`.
(Sem token → 401. Esse curl pode ser deixado para o bug bash, Task 14.)

- [ ] **Step 3: Listar funções para confirmar deploy**

Via MCP `list_edge_functions` (project_id `txvncrgkoynoxwopfkbp`).
Expected: as três funções com status `ACTIVE`.

---

## Task 10: Frontend — `src/lib/ml-oauth.ts`

**Files:**
- Create: `src/lib/ml-oauth.ts`

- [ ] **Step 1: Implementar**

```typescript
import { supabase } from './supabase';

async function chamarEdge(nome: string): Promise<Response> {
  const { data: { session } } = await supabase.auth.getSession();
  if (!session) throw new Error('Sem sessão ativa');
  const url = `${import.meta.env.VITE_SUPABASE_URL}/functions/v1/${nome}`;
  return fetch(url, {
    method: 'POST',
    headers: { Authorization: `Bearer ${session.access_token}` },
  });
}

export async function iniciarConexaoML(): Promise<void> {
  const resp = await chamarEdge('ml-oauth-start');
  if (!resp.ok) throw new Error(`Falha ao iniciar conexão ML (${resp.status})`);
  const { authUrl } = await resp.json() as { authUrl: string };
  window.location.href = authUrl;
}

export async function desconectarML(): Promise<void> {
  const resp = await chamarEdge('ml-oauth-disconnect');
  if (!resp.ok) throw new Error(`Falha ao desconectar ML (${resp.status})`);
}
```

- [ ] **Step 2: Type check**

Run: `cd "/Users/diego/Desktop/IA/Anuncios MktPlace" && pnpm exec tsc -b --noEmit`
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add src/lib/ml-oauth.ts
git commit -m "feat(m4): helpers de frontend iniciarConexaoML/desconectarML"
```

---

## Task 11: Frontend — hook `useMlConnection`

**Files:**
- Create: `src/hooks/useMlConnection.ts`

- [ ] **Step 1: Implementar**

```typescript
import { useQuery } from '@tanstack/react-query';
import { supabase } from '@/lib/supabase';

export interface EstadoConexaoML {
  conectado: boolean;
  nickname: string | null;
  mlUserId: string | null;
}

export function useMlConnection() {
  return useQuery<EstadoConexaoML>({
    queryKey: ['ml-connection'],
    queryFn: async () => {
      const { data, error } = await supabase
        .from('ml_credentials')
        .select('ml_nickname, ml_user_id, expires_at')
        .maybeSingle();
      if (error) throw error;
      if (!data) return { conectado: false, nickname: null, mlUserId: null };
      return { conectado: true, nickname: data.ml_nickname, mlUserId: data.ml_user_id };
    },
  });
}
```

- [ ] **Step 2: Type check**

Run: `cd "/Users/diego/Desktop/IA/Anuncios MktPlace" && pnpm exec tsc -b --noEmit`
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useMlConnection.ts
git commit -m "feat(m4): hook useMlConnection (estado real de conexão ML)"
```

---

## Task 12: Frontend — seção ML real em Configurações

**Files:**
- Modify: `src/pages/Configuracoes.tsx`

Substitui o card mock "Conectado / vendedor_mock" por estado real (conectar/desconectar) e mostra um banner lendo `?ml_conectado` / `?ml_erro` da URL. Sem teste (UI cosmética — regra do projeto).

- [ ] **Step 1: Reescrever os imports e o card de Mercado Livre**

Trocar o bloco de imports do topo por:
```tsx
import { useState } from 'react';
import { useSearchParams } from 'react-router-dom';
import { useQueryClient } from '@tanstack/react-query';
import { Card } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { RadioGroup, RadioGroupItem } from '@/components/ui/radio-group';
import { useMlConnection } from '@/hooks/useMlConnection';
import { iniciarConexaoML, desconectarML } from '@/lib/ml-oauth';
```

E, dentro do componente `Configuracoes`, antes do `return`, adicionar:
```tsx
  const { data: conexao, isLoading: carregandoConexao } = useMlConnection();
  const queryClient = useQueryClient();
  const [searchParams] = useSearchParams();
  const [erroAcao, setErroAcao] = useState<string | null>(null);

  const mlConectado = searchParams.get('ml_conectado') === 'true';
  const mlErro = searchParams.get('ml_erro');

  async function handleConectar() {
    setErroAcao(null);
    try {
      await iniciarConexaoML();
    } catch (e) {
      setErroAcao(e instanceof Error ? e.message : 'Falha ao conectar');
    }
  }

  async function handleDesconectar() {
    setErroAcao(null);
    try {
      await desconectarML();
      await queryClient.invalidateQueries({ queryKey: ['ml-connection'] });
    } catch (e) {
      setErroAcao(e instanceof Error ? e.message : 'Falha ao desconectar');
    }
  }
```

- [ ] **Step 2: Substituir o `<Card>` de Mercado Livre**

Trocar o card atual (linhas ~12-23 do arquivo original) por:
```tsx
        <Card className="p-4">
          <h2 className="mb-2 text-sm font-semibold">Mercado Livre</h2>

          {mlConectado && (
            <p className="mb-2 rounded bg-green-50 px-2 py-1 text-xs text-green-700">
              Conta conectada com sucesso.
            </p>
          )}
          {mlErro && (
            <p className="mb-2 rounded bg-red-50 px-2 py-1 text-xs text-red-700">
              {mlErro === 'state'
                ? 'Sessão de conexão expirou. Tente conectar de novo.'
                : 'Não foi possível conectar ao Mercado Livre. Tente de novo.'}
            </p>
          )}
          {erroAcao && (
            <p className="mb-2 rounded bg-red-50 px-2 py-1 text-xs text-red-700">{erroAcao}</p>
          )}

          {carregandoConexao ? (
            <span className="text-sm text-muted-foreground">Carregando…</span>
          ) : conexao?.conectado ? (
            <div className="flex items-center justify-between">
              <div className="flex items-center gap-2">
                <Badge className="bg-green-100 text-green-800 hover:bg-green-100">Conectado</Badge>
                <span className="text-sm">como {conexao.nickname ?? conexao.mlUserId}</span>
              </div>
              <Button variant="outline" size="sm" onClick={handleDesconectar}>
                Desconectar
              </Button>
            </div>
          ) : (
            <div className="flex items-center justify-between">
              <span className="text-sm text-muted-foreground">Nenhuma conta conectada.</span>
              <Button size="sm" onClick={handleConectar}>
                Conectar Mercado Livre
              </Button>
            </div>
          )}
        </Card>
```

- [ ] **Step 3: Type check + lint**

Run: `cd "/Users/diego/Desktop/IA/Anuncios MktPlace" && pnpm exec tsc -b --noEmit && pnpm lint`
Expected: sem erros.

- [ ] **Step 4: Commit**

```bash
git add src/pages/Configuracoes.tsx
git commit -m "feat(m4): seção ML real em Configurações (conectar/desconectar + status)"
```

---

## Task 13: Suíte completa + build

**Files:** nenhum

- [ ] **Step 1: Rodar todos os testes**

Run: `cd "/Users/diego/Desktop/IA/Anuncios MktPlace" && pnpm test`
Expected: tudo verde (os 101 anteriores + 5 novos de `auth-url`/`refresh-decisao` = 106).

- [ ] **Step 2: Build de produção**

Run: `cd "/Users/diego/Desktop/IA/Anuncios MktPlace" && pnpm build`
Expected: build OK, sem erros de tipo.

- [ ] **Step 3: Commit (se houver ajustes)** — só se algo precisou mudar para passar.

---

## Task 14: Bug bash manual (OAuth real) + docs

**Files:**
- Modify: `docs/TASKS.md`, `docs/ROADMAP.md`, `CLAUDE.md` (status)

> Depende do deploy (Task 9) e dos secrets (Task 0). Push da branch para disparar o auto-deploy do Render antes de testar pela URL pública.

- [ ] **Step 1: Push da branch + merge/deploy**

O frontend testa pela URL do Render. Garantir que a branch está publicada (merge em `main` ou deploy de preview, conforme o fluxo do Render).

- [ ] **Step 2: Fluxo de conexão ponta-a-ponta**

Em `https://ean2marketplace-frontend.onrender.com/#/configuracoes`, clicar **Conectar Mercado Livre** → autorizar na tela do ML → confirmar retorno com banner verde e badge "Conectado" + nickname real.
Verificar no banco: `SELECT user_id, ml_user_id, ml_nickname, scope, expires_at FROM ml_credentials;` (via MCP) → 1 linha, `expires_at` ~6h à frente.

- [ ] **Step 3: Casos de erro**

Abrir manualmente `…/#/configuracoes?ml_erro=state` e `…?ml_erro=token` → confirmar banners amigáveis. Confirmar que `state` é uso único (recarregar o callback não reconecta).

- [ ] **Step 4: Disconnect**

Clicar **Desconectar** → badge some, volta ao estado "Nenhuma conta conectada". Verificar no banco que a linha sumiu e que os segredos do Vault foram removidos: `SELECT count(*) FROM vault.secrets WHERE name LIKE 'ml_%';` → 0.

- [ ] **Step 5: Atualizar docs**

Marcar no `docs/TASKS.md` (seção M4 → OAuth Mercado Livre) as tarefas concluídas; anotar desvios (camada Vault reaproveitada do M2; sem testes de orquestração por restrição do vitest). Atualizar a tabela de progresso e a data. Atualizar `CLAUDE.md` (histórico + status do bloco OAuth do M4). Atualizar `docs/ROADMAP.md` se necessário.

- [ ] **Step 6: Commit**

```bash
git add docs/TASKS.md docs/ROADMAP.md CLAUDE.md
git commit -m "docs(m4): bloco OAuth ML concluído (bug bash aprovado)"
```

---

## Self-review (preenchido na escrita)

**Cobertura do spec:**
- Tela conectar/desconectar/status → Task 11+12. ✅
- `ml-oauth-start` / `callback` / `disconnect` → Tasks 6/7/8. ✅
- Vault → reaproveitado (M2) + `delete_ml_credentials` Task 1. ✅
- `getValidAccessToken` + lock → Task 5 (decisão pura testada na Task 4). ✅
- `state` via Redis TTL → Tasks 6/7 + `redisSetNX`/state. ✅
- Tratamento de erro (state/token) → Task 7 + banners Task 12. ✅
- Secrets do Supabase → Task 0. ✅
- Critérios de sucesso → cobertos no bug bash (Task 14). ✅

**Placeholders:** nenhum — todo passo tem código/comando completo.

**Consistência de tipos:** `montarAuthUrl(state, clientId, redirectUri)`, `precisaRenovar(expiresAtMs, agoraMs, bufferMs)`, `getValidAccessToken(userId)`, `TokenML`, RPCs `upsert_ml_credentials`/`get_ml_tokens`/`delete_ml_credentials` com as assinaturas reais do schema — consistentes entre tasks.
