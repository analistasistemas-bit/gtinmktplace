# Delete list report

- Removidos três comentários redundantes de `src/lib/mensagens.ts`, sem alterar expressões.
- Removido o setup morto de token no teste de pedido cancelado; o teste continua exigindo zero chamadas ao token.
- Validação: `vitest` direcionado e `git diff --check` executados com sucesso.
