# CLAUDE.md — Contexto persistente do projeto PubliAI

> Este arquivo é o **system prompt do projeto**. Toda sessão de Claude Code (ou outro agente IA) deve lê-lo primeiro, antes de tocar em qualquer arquivo. Ele resume regras, convenções, decisões e workflow do projeto, e aponta para a documentação detalhada.

**Última atualização:** 2026-06-09
**Status do projeto:** M0+M1+M2+M3+M3.1 ✅ concluídos; M4 (Integração ML) em andamento — **OAuth ✅** + **concorrência ✅** + **estratégia de preço ✅** + **categorias/atributos ✅** + **potencial de venda ✅** (process-familia v18) + **pré-condição ADR-0013 ✅** + **Publicação CREATE ✅ validada com token real** (bug bash 2026-06-04: 2 anúncios reais publicados; `publish-familia-ml` v8, `publicar-familias` v3 — GTIN/EMPTY_GTIN_REASON, foto via `/pictures`, descrição sanitizada, capa nas variações, Clássico/Premium no modal) + **card "Você recebe por venda" ✅** (comissão exata Clássico/Premium via edge `calcular-tarifa-ml` v1 + alerta de frete); próximo: **Publicação UPDATE** (ADR-0005)

---

## O que é este projeto

**PubliAI** é um sistema web interno que transforma planilhas de produtos exportadas do sistema interno da empresa em **anúncios publicados no Mercado Livre**, usando IA como copywriter especializado em **aviamentos** (linha de costura, botões, fitas).

- **Cliente final:** empresa de tecidos e aviamentos (uso interno)
- **Desenvolvedor único:** Diego — funcionário interno; também é o usuário-operador-validador
- **Substitui:** uma proposta comercial externa (Leonardo Freitas) que foi inviável financeiramente
- **MVP foca em:** aviamentos no Mercado Livre. Tecidos e outros marketplaces ficam para v2+
- **Prazo:** 10 semanas de desenvolvimento (~2,5 meses), com buffer de 3 semanas até o limite de 3 meses

---

## Por onde começar (todo agente lê isso)

Antes de fazer **qualquer alteração** ou tomar **qualquer decisão**, consulte os documentos abaixo nesta ordem:

1. [docs/README.md](docs/README.md) — índice geral
2. [docs/superpowers/specs/2026-05-26-publiai-design.md](docs/superpowers/specs/2026-05-26-publiai-design.md) — **spec completo** do design
3. [docs/ROADMAP.md](docs/ROADMAP.md) — em que marco estamos, o que está fora de escopo
4. [docs/TASKS.md](docs/TASKS.md) — checklist operacional; pegue a próxima tarefa aqui
5. [docs/decisions/](docs/decisions/) — todos os ADRs vigentes; **para qualquer decisão técnica, leia o ADR correspondente antes de propor algo diferente**

Se algum desses arquivos ainda não cobre algo que você precisa decidir, **escreva um ADR novo antes de implementar**.

---

## Stack confirmado

(detalhes completos em [ADR-0001](docs/decisions/0001-stack-tecnologico.md))

| Camada | Tecnologia |
|---|---|
| Frontend | Vite + React 18 + TypeScript + shadcn/ui + Tailwind + TanStack Query + Zustand |
| Backend / DB / Storage / Auth / Realtime | Supabase (Postgres + Edge Functions Deno/TS + Storage + Vault) |
| Hospedagem do frontend | Render Static Site |
| Fila assíncrona | Upstash QStash |
| Cache | Upstash Redis |
| IA copywriting | OpenAI GPT-4o-mini |
| IA visão (cor) | OpenAI GPT-4o Vision |
| Integração externa | Mercado Livre API (OAuth 2.0) |

**Custo operacional alvo:** $3–28/mês

---

## MCPs do ambiente (priorizar SEMPRE)

Diego tem MCPs configurados localmente; usá-los é a primeira escolha em qualquer operação que tenha equivalente:

- `supabase-mcp-server` — operações no Supabase (DB, auth, storage, edge functions)
- `upstash` — QStash + Redis
- `render` — deploy do frontend
- `shadcn` — componentes UI
- `context7` — documentação atualizada de SDKs (use antes de chutar API)
- `n8n-mcp`, `Firebase`, `Stitch` — disponíveis se necessário

**Regra de ouro:** antes de propor solução custom ou sugerir "vá no dashboard do serviço X", verifique se há MCP equivalente.

---

## Decisões arquiteturais já tomadas (todos os ADRs)

Quando o assunto for um destes tópicos, **leia o ADR antes de propor mudança**:

| ADR | Tópico |
|---|---|
| [0001](docs/decisions/0001-stack-tecnologico.md) | Stack tecnológico |
| [0002](docs/decisions/0002-mvp-aviamentos-primeiro.md) | MVP começa por aviamentos, não tecidos |
| [0003](docs/decisions/0003-variacoes-agrupadas-por-pai.md) | Variações agrupadas por código PAI no anúncio do ML |
| [0004](docs/decisions/0004-atribuicao-de-cor.md) | Cor: descrição primeiro, IA Vision como fallback |
| [0005](docs/decisions/0005-lifecycle-publish-and-update.md) | Lifecycle: publica novo, atualiza existente |
| [0006](docs/decisions/0006-qstash-em-vez-de-postgres-queue.md) | QStash em vez de fila no Postgres |
| [0007](docs/decisions/0007-modelo-de-dados-4-tabelas.md) | Modelo de dados: 4 tabelas, sem catalogo_interno separado |
| [0008](docs/decisions/0008-estrategia-de-preco-condicional.md) | Estratégia de preço condicional (PRÓPRIO vs COMPETITIVO) |
| [0009](docs/decisions/0009-campos-payload-ml-e-categoria-deterministica.md) | Campos obrigatórios do payload ML + categoria via lookup determinístico (não via IA) |
| [0010](docs/decisions/0010-openrouter-em-vez-de-openai-direto.md) | IA via OpenRouter (gateway compatível com OpenAI SDK), não OpenAI direto |
| [0011](docs/decisions/0011-redirect-uri-via-edge-function.md) | OAuth ML: redirect URI aponta para Supabase Edge Function (não para o frontend; mantém client_secret no servidor e evita o problema do HashRouter) |
| [0012](docs/decisions/0012-refresh-token-oauth-ml-com-lock-redis.md) | Refresh de token OAuth ML com lock distribuído no Redis (evita corrida de refresh) |
| [0013](docs/decisions/0013-edge-cases-da-planilha-no-ingest.md) | Edge cases da planilha no ingest (não-bloqueantes): CODIGO duplicado → manter 1ª + avisar; filho órfão → pular + avisar; PAI sem filho → pular + avisar |
| [0014](docs/decisions/0014-busca-de-concorrencia.md) | Busca de concorrência no ML: 1 busca por família; GTIN válido → catálogo, senão título (baixa confiança); classe sem/moderada/alta só informativa; erro → PRÓPRIO seguro |
| [0016](docs/decisions/0016-publicacao-update-reposicao-estoque.md) | Publicação UPDATE: reposição de estoque herdando o anúncio anterior sem IA; só estoque (preço/título/fotos preservados); mudança estrutural detecta+sinaliza; PUT inclui todas as variações para não deletar (refina 0005) |
| [0017](docs/decisions/0017-selo-de-desconto-via-api-de-promocoes.md) | Selo "% OFF" exige a API `/seller-promotions` (PRICE_DISCOUNT), não `original_price` (descontinuado). **Estacionado**: app sem permissão de promoções (403 PolicyAgent) + AVILBV sem reputação verde/vendas. Infra (tabela `configuracoes`, flags, UI) mantida dormente |
| [0018](docs/decisions/0018-dimensoes-e-peso-no-payload-ml.md) | Dimensões/peso reais no payload via atributos `SELLER_PACKAGE_*` (cm/g) p/ frete correto (evita moderação "serviço no frete"). CREATE + UPDATE; guarda `dimensoesValidas` contra o placeholder 0,1cm da planilha; dado inválido → publica sem dimensão + aviso na Revisão (não bloqueia) |
| [0019](docs/decisions/0019-exclusao-lote-preserva-publicados.md) | Exclusão de lote preserva famílias publicadas (mantém vínculo de UPDATE + tela Publicados); ML nunca tocado; guarda contra race do QStash; contadores recontados na edge (trigger não cobre DELETE); Storage limpo manualmente. Tela "Publicados" com status ao vivo (`GET /items?ids=`) + escape hatch "remover do sistema" |
| [0020](docs/decisions/0020-estrategia-de-preco-liquido-minimo.md) | PRECO = líquido mínimo após comissão ML; com concorrente preço = menor × 0,95, sem concorrente gross-up até cobrir o piso; arredonda múltiplos de R$ 0,05; semáforo verde/amarelo/vermelho de viabilidade (substitui ADR-0008) |
| [0021](docs/decisions/0021-vinculacao-automatica-ao-catalogo-ml.md) | Vinculação automática ao Catálogo do ML no CREATE/UPDATE: opt-in por variação (`POST /items/catalog_listings`) travado por elegibilidade (`READY_FOR_OPTIN`) e match só por GTIN real; `FAMILY_DIFF` (anúncio multi-família) não vincula; best-effort/idempotente; aviamentos são `catalog_required` mas não `catalog_only` (marketplace sobrevive) |
| [0022](docs/decisions/0022-categoria-cola-e-seletor-manual.md) | Novo tipo determinístico `cola` → categoria `MLB277319` "Bastões de Cola" (BRAND+MODEL, expõe EMPTY_GTIN_REASON); estende ADR-0009. Seletor manual de categoria na Revisão (edge `definir-categoria-familia` reusando `_shared`) como escape hatch p/ produtos `outro` |

---

## Convenções do projeto

### Documentação
- **Toda decisão técnica não-trivial** vira ADR em `docs/decisions/` antes da implementação
- ADRs são **imutáveis** depois de aceitos; se mudar, criar novo ADR substituindo (com referência)
- `docs/ROADMAP.md` e `docs/TASKS.md` são **vivos** — atualize ao avançar
- Atualizar `docs/README.md` e este `CLAUDE.md` quando criar ADR novo ou mudar workflow

### Código
- **TypeScript strict mode** sempre
- **Tipos do Supabase gerados** (`supabase gen types`) — não escrever à mão
- **Camada de IA isolada** (não chamar OpenAI direto em todo lugar — passar por `lib/ai/*`) para facilitar swap de modelo
- **Camada de fila isolada** (`lib/queue.ts`) — abstrair QStash para que troca seja localizada
- Edge Functions devem ser **idempotentes** (verificar `status` antes de processar — padrão completo em [ADR-0006](docs/decisions/0006-qstash-em-vez-de-postgres-queue.md))
- **Sem comentários explicando o "o que"** — só o "porquê" quando não-óbvio
- **Sem código morto** — funcionalidades não-pedidas ficam de fora
- **UI / shadcn (React 18):** o projeto roda **React 18**, mas os componentes shadcn estilo `radix-nova` (`components.json`) são gerados para **React 19** (ref como prop comum). Todo primitivo interativo em `src/components/ui/` que renderiza um elemento host (button/input/textarea/span…) **DEVE usar `React.forwardRef`** — senão triggers `asChild` que dependem do ref (ex.: `DropdownMenuTrigger` → Popper) quebram **só no navegador** (o jsdom não pega). Ao adicionar/regenerar um primitivo via CLI do shadcn, conferir o `forwardRef`. Guard: `tests/components/ui-ref-forwarding.test.tsx`.

### Banco de dados
- **4 tabelas:** `lotes`, `familias`, `variacoes`, `ml_credentials`
- **RLS por user_id** em todas as tabelas de domínio
- **Tokens OAuth** sempre via Supabase Vault (pgsodium)
- **Enums Postgres** em vez de string livre
- **Migrations aditivas** sempre que possível; nunca soltar `DROP COLUMN` sem backup

### Arquivos e imagens
- Imagens de produto: arquivo nomeado `00CODIGO.jpeg` (8 dígitos, zero-padded)
- Storage path: `{user_id}/{codigo}.jpeg` no bucket `imagens`
- Capa do anúncio agrupado: foto do PAI se existir o arquivo; caso contrário, foto da primeira variação encontrada
- 2ª foto comum da família: arquivo `CAPA2_00CODIGO.ext` (ou upload na Revisão) → entra como 2ª foto de todas as variações (CREATE e UPDATE)
- Variação principal: escolhida na Revisão (só CREATE); é a 1ª variação enviada ao ML (o ML usa a ordem do array)

### Schema esperado da planilha
Colunas obrigatórias: `CODIGO`, `PAI`, `NOME`, `UNIDADE`, `GTIN`, `CUSTO`, `PRECO`, `ESTOQUE`, `DESCRICAO_DETALHADO`, `PESO_GRAMAS`, `ALTURA_CM`, `LARGURA_CM`, `COMPRIMENTO_CM`, `FORNECEDOR`.

Regras:
- `PAI = 0` significa que o produto é o pai (não vendido, só agrupador)
- `PAI = <código>` significa que o produto é filho daquele código
- Se faltar coluna obrigatória, o ingest falha cedo (erro de validação clara)
- `CUSTO` é o custo do produto (por variação); usado para markup. `FORNECEDOR` (por família, da linha PAI) vira a marca/BRAND do anúncio (fallback "Avil")

### Domínio
- **Produtos PAI nunca são vendidos** — só conceito de agrupador no ML
- **Filhos (cores) são os SKUs reais** que viram variações dentro do anúncio único do ML
- **Revisão humana obrigatória** antes de qualquer publicação no ML

---

## Workflow por sessão

Ao iniciar uma sessão neste projeto:

1. Ler este CLAUDE.md
2. Ler `MEMORY.md` global do Claude Code (preferências persistentes de Diego)
3. Ler `docs/ROADMAP.md` (em que marco estamos)
4. Pegar próxima tarefa pendente em `docs/TASKS.md`
5. Se for tarefa de implementação:
   - Conferir ADR relacionado (se houver)
   - Implementar
   - Atualizar TASKS.md (marcar ✅)
   - Se a tarefa originou decisão nova → escrever ADR
   - Se mudou roadmap → atualizar ROADMAP.md
   - Antes do commit: lint + types check
6. Se for tarefa de design/planejamento:
   - Seguir workflow do Superpowers (`brainstorming` → `writing-plans` → `subagent-driven-development`)
7. Sempre que tomar atalho ou pular convenção: registrar exceção como TODO no TASKS.md ou em comentário no código (com explicação)

---

## Regras de operação para a IA

Estas regras são **inegociáveis** salvo aprovação explícita do Diego em mensagem da sessão:

1. **Antes de propor algo, ler ADR relacionado.** Não improvisar contradizendo ADR vigente.
2. **Antes de implementar decisão não-trivial, escrever ADR.** ADR vem antes do código, não depois.
3. **Priorizar MCPs configurados** sobre soluções manuais ou outros serviços.
4. **Recomendar diretamente** em decisões técnicas que Diego não tem expertise; menu A/B/C/D só em decisões de produto/negócio.
5. **Caminho mais prático e rápido** vence elegância em tradeoffs (Diego declarou preferência).
6. **Edge Functions sempre idempotentes** — verificar status antes de processar.
7. **Para mudanças que afetam arquitetura**: atualizar spec + criar ADR antes de codar.
8. **Cada commit** deve fazer referência a item do TASKS.md ou ADR.
9. **Testes:** TDD onde aplicável (regras de negócio, parsers, cálculo de preço); pular onde não agrega valor (UI cosmética).
10. **Authoring e review separados** (regra do OMC): nunca aprovar a própria mudança no mesmo passo.

---

## Glossário

| Termo | Significado |
|---|---|
| **PAI** | Produto-pai conceitual, identificado pelo campo `PAI=0` na planilha; agrupa variações; nunca é vendido isoladamente |
| **Filho** | Produto-variação (geralmente uma cor); tem `PAI = <código do pai>`; é o SKU real vendido |
| **Família** | Registro do PAI no nosso banco (tabela `familias`); vira **1 anúncio** no ML |
| **Variação** | Registro do filho no nosso banco (tabela `variacoes`); vira **1 variação** dentro do anúncio do ML |
| **Lote** | Uma sessão de importação (1 upload de planilha + imagens correspondentes) |
| **CREATE / UPDATE** | Operações de publicação no ML: CREATE = anúncio novo; UPDATE = atualizar estoque/preço de anúncio existente |
| **Operador** | O usuário-final do sistema (no caso, Diego ou outra pessoa interna da empresa) |
| **GTIN** | Código de barras EAN; pode ser nulo ou começar com `3000*` (código interno, não EAN real GS1) |
| **MLB####** | Identificador de categoria no Mercado Livre |

---

## O que NUNCA fazer neste projeto

- ❌ **Inventar dados** que a IA não tem (alucinar especificações inexistentes — prompt do copywriter proíbe explicitamente)
- ❌ **Pular o operador** (revisão humana é obrigatória antes de qualquer publicação no ML)
- ❌ **Quebrar idempotência** das Edge Functions (sempre verificar status antes de processar)
- ❌ **Salvar tokens OAuth em texto puro** (sempre via Supabase Vault)
- ❌ **Ignorar RLS** (sempre policy por user_id em toda tabela de domínio)
- ❌ **Criar tabelas novas** sem ADR aprovado
- ❌ **Pôr credenciais** em código ou no repo — sempre via `.env.local` (não commitado) ou Supabase secrets
- ❌ **Modificar ADR** depois de aceito (criar novo substituindo)
- ❌ **Recomendar serviço externo** se houver MCP equivalente já configurado
- ❌ **Fazer review da própria mudança** no mesmo passo (sempre passes separados — `code-reviewer` ou `verifier` agente)
- ❌ **Quebrar a regra do PAI**: PAI nunca é vendido isoladamente; sempre publicado como agrupador
- ❌ **Auto-publicar sem revisão**, mesmo em casos UPDATE — operador sempre confirma o lote

---

## Trilho paralelo — App Mercado Livre Developers ✅

App PubliAI criada no portal ML Developers em 2026-05-27. Credenciais (`ML_CLIENT_ID`, `ML_CLIENT_SECRET`, `ML_REDIRECT_URI`) salvas no `.env.local`. Redirect URI aponta para Supabase Edge Function `ml-oauth-callback` (a ser criada em M4) — ver [ADR-0011](docs/decisions/0011-redirect-uri-via-edge-function.md).

Tarefas que sobram do trilho são naturais do M4 (validar fluxo OAuth real + confirmar rate limits). Certificação formal da app **não é necessária** porque o uso é interno (PubliAI publica nos anúncios da própria Daludi).

---

## Histórico deste CLAUDE.md

| Data | Mudança |
|---|---|
| 2026-05-26 | Criação inicial após brainstorming completo (Seções 1-6 aprovadas, 8 ADRs, ROADMAP, TASKS, spec consolidado) |
| 2026-05-27 | M0+M1+M2 todos ✅; status atualizado pra refletir início do M3 |
| 2026-05-27 | Trilho paralelo ML ✅ + ADR-0011 (redirect URI via Edge Function) registrado |
| 2026-05-28 | M3 ✅ via Plano 04 (20 tasks Subagent-Driven Development). Pipeline IA completo: parser cor PT-BR, Vision (gpt-4o conservador), Copywriter (gpt-4o-mini + json_schema), cache Redis TTL 90d. UI: badges cor_origem, drop zone, ícone câmera. Edge functions deployadas: process-familia v11, upload-imagens-lote v1, invalidar-cache-cor v1. Bug bash com 4 famílias reais + 5 ajustes no prompt → Diego aprovou. 86 testes passando. Status do projeto agora aponta para M4 (Integração ML). |
| 2026-05-28 | M3.1 entregue: foto-capa por família (spec + plano-05 + 12 tasks subagent-driven) + 6 ajustes pós (barra progresso em chunks, novo template emoji da descrição, botão regenerar por família, badge compacto com tooltip, GTIN editável). Edge functions deployadas: process-familia v12 (template novo), upload-imagens-lote v5 (CAPA_), regenerar-copy-familia v1 (nova). 101/101 testes verdes. |
| 2026-05-29 | M4 bloco OAuth ML ✅ (spec + ADR-0012 + plano-06 15 tasks subagent-driven). Edge functions deployadas `verify_jwt:false`: ml-oauth-start, ml-oauth-callback, ml-oauth-disconnect. `getValidAccessToken` com refresh proativo + lock Redis (ADR-0012, resolve gap §541). Vault reaproveitado do M2 + migration nova `delete_ml_credentials`. Bug bash real aprovado (conectou como AVILBV; disconnect limpa Vault). Bugs corrigidos no bug bash: domínio de auth do Brasil é `mercadolivre.com.br` (com "v"); banner de sucesso preso pós-disconnect. 106/106 testes verdes. TODO: instalar eslint (`pnpm lint` quebrado). |
| 2026-05-31 | ESLint instalado e configurado (toolchain ESLint 9 flat config + `eslint.config.js`); `pnpm lint` passa (0 errors, 3 warnings benignos). TODO do M4 resolvido. |
| 2026-05-31 | TASKS.md sincronizado com o código real (falsos-pendentes marcados ✅). ADR-0013 registra a política de edge cases da planilha no ingest (gap §556): os 3 casos (CODIGO duplicado, filho órfão, PAI sem filho) passam a ser não-bloqueantes (descartar + avisar); hoje os 2 últimos rejeitam o lote. Tabela de ADRs deste arquivo atualizada com 0012 e 0013. |
| 2026-06-01 | M4 busca de concorrência Tasks 8+9 ✅: migration `add_concorrencia_familias` aplicada via MCP (2 enums `origem_concorrencia`/`classe_concorrencia` + 4 colunas `concorrencia_*` em `familias`), tipos regenerados. `process-familia` integra `buscarConcorrencia` 1×/família após a copy (resiliente: erro → `nenhuma`/PRÓPRIO seguro); inclui `gtin` no select das variações e persiste os 4 campos. 122→125 testes verdes, lint verde. |
| 2026-06-01 | **Bug bash da concorrência (Task 10) — achado crítico + correção.** Com token real (AVILBV reconectado), `/sites/MLB/search` retorna **403** (ML descontinuou o search de itens por site). Corrigido para o **catálogo** (intenção original do ADR-0014): ramo GTIN = `/products/search?q={gtin}` → `/products/{id}/items` (conta `seller_id` distintos + `min(price)`); ramo título não quantifica (catálogo textual = ~10k ruído) → `origem='titulo'`/PRÓPRIO seguro. `parse.ts` agora expõe `parseProdutoBusca`+`parseItensProduto`. `process-familia` **v15** deployada. ADR-0014 aditado (§Adendo). Validado contra API real: GTIN `7891521360659` → produto `MLB34175726`, 6 vendedores, R$ 12,62, classe alta. |
| 2026-06-01 | **Busca de concorrência concluída (Task 10 ✅).** Bug bash do **lote #5** (4 famílias) validou o pipeline v15 ponta a ponta com token real: FITA N.3 → 6 vend./R$12,62; LINHA XIK → 6/R$12,90; FITA N.9 → 9/R$17,99 (todas `origem=gtin`/alta); LINHA 1500MT (GTIN `4201516783012` fora do catálogo ML, `paging.total=0`) → `gtin`/0 vendedores, status `pronto` (resiliência validada). Ramo título não exercitado no lote (todas tinham EAN) — lógica é retorno trivial. **Nota operacional:** o MCP `qstash_publish_message` não aciona o `process-familia` (conta QStash divergente das signing keys do Supabase); reprocessar exige lote novo pela UI (`ingest-lote` usa o SDK QStash real). |
| 2026-06-01 | **Estratégia de preço condicional ✅ (ADR-0008).** Função pura `calcularEstrategiaPreco` (`_shared/preco/calcular.ts`, TDD 6 testes): vendedores 0/sem preço_min → PRÓPRIO; preço_min ≤ planilha → COMPETITIVO (preço_min − R$ 0,01); preço_min > planilha → PRÓPRIO ("já menor"). `process-familia` **v16** persiste `preco_publicacao` por variação (preserva `preco_editado_pelo_operador`) + `estrategia_preco`/`estrategia_motivo` da família. Frontend: adapters reais (concorrência, vendedores, preço_min, `precoAbaixo20pc` p/ gap §556) + linha "publica: R$ X" no card + detalhe de concorrência no expandido + util `fmtBRL`. 131 testes verdes. **Insight (projeção SQL lote #5):** a Daludi vende 2–3× mais barato que o ML → todas as famílias caem em PRÓPRIO ("já menor"); o ramo COMPETITIVO raramente dispara na prática real. A persistência v16 aparece na UI no próximo lote subido. |
| 2026-06-01 | **Mapeamento de categorias/atributos ✅ (ADR-0009 + Adendo).** Achado: os IDs do ADR (MLB1132/1430/1429) eram chutes e estavam **todos errados** (categorias raiz não-publicáveis). IDs reais validados via `domain_discovery` + `/categories/{id}` com token de produção: linha→**MLB270273**, fita→**MLB255054**, botao→**MLB270272** (folha, listing_allowed). Atributos obrigatórios reais: linha=BRAND+MODEL, fita=BRAND+RIBBON_TYPE, botao=BRAND+MATERIAL. `_shared/categoria/{detectar,atributos}.ts` (TDD 17 testes): `detectarTipoAviamento` (regex PT-BR), `categoriaParaTipo`, `montarAtributosML` (BRAND fixo "Avil", MODEL=nome, RIBBON_TYPE/MATERIAL inferidos), `atributosFaltantes`. `process-familia` **v17** popula tipo_aviamento/tipo_origem/categoria_ml_id/atributos_ml. UI: badge "categoria indefinida" quando tipo=outro; IDs corrigidos na tela Configurações. 148 testes verdes. Camada IA classificadora não implementada (regex cobre os casos reais). Próximo: publicação CREATE/UPDATE. |
| 2026-06-01 | **Fix: foto-capa (CAPA_) ignorada no upload inicial do lote** (systematic-debugging). Causa raiz: o `ingest-lote` só fazia `matchImagem` das variações (`00CODIGO.jpeg`) e **nunca tratava o prefixo `CAPA_`** — a detecção de capa (`classificarArquivo`) só existia no `upload-imagens-lote` (drop-zone posterior, M3.1). Então `CAPA_00CODIGO.jpg` subia ao storage mas `familias.capa_storage_path` ficava null. Fix: `matchCapa(codigoPai, paths)` em `_shared/parser.ts` (TDD, 4 testes) + `ingest-lote` seta `capa_storage_path` no insert das famílias (deploy v7). Dado retroativo corrigido via SQL (lotes #5/#6/#7). Só recarregar a revisão (frontend já renderizava `capaStoragePath`). 166 testes verdes. |
| 2026-06-01 | **Card "Potencial de venda"** (Superpowers completo: brainstorming → ADR-0015 → spec → plano-09 → subagent-driven 9 tasks). A venda exata por produto **não é exposta pela API do ML** (sold_quantity null; /items e /reviews 403) — investigado com token real. Usamos proxies: faixa de preço dos concorrentes, frete grátis, FULL, força dos concorrentes (MercadoLíder + maior reputação de vendas via `/users`), ranking da categoria (`/highlights`) e idade no catálogo. Backend: `parseItensProduto`→`DadosOfertas`, `analisarMercado` (`_shared/ml/mercado.ts` + `mercado-agregar.ts` puro, cache seller 24h/highlights 6h, resiliente), coluna `analise_mercado jsonb`, `process-familia` **v18**. Frontend: card no `PainelAnalise` + `fmtMilhar`. ADR-0015 + plano-09. 162 testes verdes. Falta bug bash com lote real. |
| 2026-06-03 | **Publicação CREATE no Mercado Livre implementada** (Superpowers completo: brainstorming → spec `2026-06-03-m4-publicacao-create-design.md` → plano-10 → subagent-driven, 14 tasks + correções pós-review). **Seleção do que publicar** na Revisão (pedido do Diego): granularidade família + excluir cores; `familiaPublicavel` (TDD) bloqueia incompletas com motivo; selo na `FamiliaRow`, checkbox "incluir cor" (persiste `variacoes.excluida_da_publicacao`), filtro "Incompletas", footer "Publicar selecionadas" + modal de confirmação (revisão humana). **Backend:** edge `publicar-familias` v1 (claim atômico `status='publicando'` + enfileira QStash) → worker `publish-familia-ml` v2 (idempotente via `ml_item_id`; sobe fotos `POST /pictures` com signed URL 2h + cache `capa_ml_picture_id`; `montarPayloadItem` TDD; `POST /items`; persiste `ml_item_id`/`ml_permalink`/`ml_variation_id`; 4xx persiste erro / 5xx+429 retenta; transição do lote ao fim). `Relatorio.tsx` real (links/erro/tentar de novo). Migrations: `excluida_da_publicacao`, `capa_ml_picture_id`. Code review opus pré-bug-bash: 🔴 atributos rejeitado (montarAtributosML sempre preenche defaults → "categoria definida" ≡ atributos completos), 🟠 idempotência corrigidos. 190 testes verdes. **Pendente:** bug bash com token real (descobre GTIN interno `3000*`/`listing_type_id`/forma de foto — spec §5.4). |
| 2026-06-03 | **Pré-condição da publicação CREATE: ADR-0013 implementado** (TDD). `agruparPorPai` passou de `FamiliaAgrupada[]` (com `throw` em órfão/PAI-sem-filho) para `{ grupos, anomalias }` (`ResultadoAgrupamento`): dedup por CODIGO (1ª vence) + coleta não-bloqueante de filhos órfãos e PAIs-sem-filho. `ingest-lote` aborta só se sobrar 0 família após o descarte e persiste `anomalias` na coluna nova `lotes.anomalias_planilha` (jsonb, migration `add_anomalias_planilha_lotes`). Frontend: `parseAnomalias`/`totalAnomalias` + `Lote.anomalias` no adapter + faixa âmbar de descartados no `Progresso.tsx`. TDD: `_shared/__tests__/parser.test.ts` (5) + `tests/lib/anomalias.test.ts` (5); os 3 testes antigos de comportamento bloqueante foram migrados. 173 testes verdes, build/lint verdes. **Deploy `ingest-lote` v8 confirmado via MCP (2026-06-04)** — parser local idêntico ao deployado + coluna `lotes.anomalias_planilha jsonb` aplicada. Registrado também o pedido do Diego de **seleção granular do que publicar** na Revisão (vai para o brainstorming do bloco CREATE). |
| 2026-06-04 | **Bug bash da Publicação CREATE ✅ (2 anúncios reais publicados com token AVILBV).** Diversos bugs/melhorias descobertos e corrigidos ao vivo: (1) **frontend desatualizado no Render** — `main` local estava 20 commits à frente do GitHub; push destravou o deploy; (2) **GTIN sem EAN** — placeholder inválido `EMPTY_GTIN_NUMBER` trocado pelo atributo real `EMPTY_GTIN_REASON` (`value_id 17055160`, validado via `/categories/{id}/attributes`); só linha/fita expõem o atributo, botão omite (GTIN é `conditional_required`); GTIN preenchido-mas-malformado vai literal ao ML (pós-review Codex); (3) **upload de foto** — `/pictures/items/upload` dava 405 (tengine); corrigido para `POST /pictures`; (4) **retry travado** — família `erro` voltou a ser publicável (`familiaPublicavel` aceita `erro`; edge `publicar-familias` claim `in ('pronto','erro')` + limpa `erro_mensagem`); (5) **descrição** — não era enviada (recurso separado no ML); `garantirDescricaoML` (POST→PUT) + **sanitização de emojis** (ML rejeita emojis na descrição com `DESCRIPTION_PLAIN_TEXT_NOT_ALLOWED`; preview mantém emojis, envio ao ML limpa); (6) **foto-capa não aparecia em anúncio com variações** — o ML exibe galeria por variação; capa entra como 1ª `picture_id` de cada cor. **Melhorias UX pedidas pelo Diego:** preço replica entre cores da família; campo de preço passou a editar o **preço de publicação** (conserta o "revert"); **escolha Clássico/Premium no modal** de publicação (`listing_type_id` viaja modal → `publicarFamilias` → edge → job QStash → worker → `montarPayloadItem`). Workers redeployados via MCP: `publish-familia-ml` v8, `publicar-familias` v3. ~206 testes verdes. |
| 2026-06-04 | **Card "Você recebe por venda" na Revisão** (Superpowers completo: brainstorming → spec `2026-06-04-card-voce-recebe-revisao-design.md` → plano → subagent-driven, 6 tasks + fix pós-review). Mostra, por família, **comissão exata (% + tarifa fixa) e o líquido**, comparando **Clássico vs Premium** lado a lado, no Painel de Análise (grid 2 colunas com Potencial de venda). **Investigação da estrutura de custos do ML:** comissão é obtível com precisão via `/sites/MLB/listing_prices` (`sale_fee_amount` = %+fixa; a fixa só em itens < ~R$29 e é pesada — R$12,50 → comissão ~62%); o **frete logístico NÃO é obtível** (endpoints de custo do vendedor dão 403/`PolicyAgent`; o que funciona devolve frete cheio sem os subsídios do ML), então é tratado como **alerta genérico** ("acima de R$19 o ML dá frete grátis por sua conta"). Backend: `montarTarifa` puro (TDD) + edge `calcular-tarifa-ml` v1 (2× listing_prices Clássico/Premium, cache Redis 6h, resiliente → card "indisponível" em falha). Frontend: `calcularTarifaML` + hook `useTarifaML` (recalcula ao mudar o preço) + `CardVoceRecebe`. Fix pós-review: card usa o **preço de publicação** (`precoPublicacao ?? preco`), não o da planilha. **Bug bash com token real: percentual e líquido batendo.** Sem coluna nova no banco (cálculo on-demand). |
| 2026-06-01 | **Painel de Análise visual na Revisão** (Superpowers completo: brainstorming → spec → plano-08 → execução inline). A pedido do Diego, estratégia/concorrência/categoria saíram do final do expandido (texto corrido) para um `PainelAnalise` visual no topo, ao lado da foto-capa: cards com ícones (Coins/Tag/Store) + cores semânticas (PRÓPRIO=azul, COMPETITIVO=âmbar; concorrência sem=cinza/moderada=azul/alta=âmbar) + alerta de preço perigoso consolidado + nome amigável de categoria. Label PRÓPRIO acentuado (enum é 'PROPRIO'). Só frontend (zero backend/schema). Spec `2026-06-01-painel-analise-revisao-design.md` + plano-08. 155 testes verdes, build/lint verdes. |
| 2026-06-05 | **CUSTO + FORNECEDOR + markup** (Superpowers completo: brainstorming → spec `2026-06-05-custo-fornecedor-markup-design.md` → plano → subagent-driven, 12 tasks). A planilha ganhou duas colunas: **CUSTO** (por variação) e **FORNECEDOR** (por família, da linha PAI). **BRAND deixa de ser fixo "Avil"** → usa o FORNECEDOR (fallback "Avil"); `montarAtributosML(tipo, nome, marca)`, aplicado no `process-familia` (só CREATE; UPDATE preserva atributos por ADR-0016). **Markup** exibido no card "Você recebe" por tipo (Clássico/Premium): `lucro = líquido − custo`, `markup = lucro/custo` (sobre o líquido, após comissão); prejuízo em vermelho. Custo representativo = variação de menor preço de publicação. Migration aditiva `variacoes.custo`/`familias.fornecedor` (distinto de `custo_centavos` = custo de IA); ingest persiste ambos em CREATE+UPDATE. Função pura `calcularMarkup` (TDD). Adendo ao ADR-0009. 238 testes verdes, build/lint verdes. |
| 2026-06-05 | **Variação principal + 2ª foto comum** (Superpowers completo: brainstorming → spec `2026-06-05-variacao-principal-segunda-foto-design.md` → plano → subagent-driven, 14 tasks). **Variação principal** (só CREATE): o operador escolhe na Revisão qual cor é a principal (`familias.variacao_principal_codigo`); o ML define pela ordem, então `publish-familia-ml` ordena a principal primeiro (`ordenarVariacoesPrincipal`). **2ª foto comum** (CREATE+UPDATE): espelha a capa — prefixo `CAPA2_00CODIGO` no lote (`classificarArquivo`/`matchCapa2`) + botão "Subir 2ª foto" na Revisão (`subirCapa2Familia`); entra como 2ª `picture` de toda variação e no `item.pictures`. No UPDATE, propaga a capa2 às variações existentes reenviando `picture_ids = [capa, capa2, própria]` (adendo ADR-0016). Migration aditiva `familias.capa2_storage_path`/`capa2_ml_picture_id`/`variacao_principal_codigo`. `montarPayloadItem`/`montarVariacaoNova` recebem capa2; `montarVariacoesUpdate` aceita `picsPorCodigo`. Adendos ADR-0003 e ADR-0016. 250 testes verdes. |
| 2026-06-05 | **Código da cor nas variações** (Superpowers completo, branch `feat/codigo-da-cor`). Para produtos cujo NOME traz `{código} {cor}` (ex.: `FITA ... 1354 VERMELHO TOMATE 10MT`), `extrairCorECodigo` (`_shared/cor/extrair.ts`, parser de tokens: último dígito-puro seguido de só-letras = código; palavras só-letras seguintes = cor literal, até o tamanho) extrai código + nome comercial exato, expande abreviações (AZ→Azul, VD→Verde, AMA→Amarelo, CL→Claro, ESC→Escuro, BCA→Branco, PTO→Preto) + title-case, embutindo em `variacoes.cor` como `"Vermelho Tomate 1354"`. Camada 0 no `process-familia` antes do dicionário (sem código → dicionário atual). Exibido na Revisão (editável) e no COLOR do ML; sem coluna nova, sem frontend. Adendo ADR-0004. 258 testes verdes. |
| 2026-06-06 | **Acento no código da cor** (`extrairCorECodigo` ganha `ACENTOS_COR`): restaura acento por palavra após abreviações + title-case (Pétala, Orquídea, Magnólia, Botânico, Cítrico, Salmão, Bordô, Médio, Abóbora + Lilás/Caqui/Petróleo/Bebê/Café/Limão/Índigo). TDD; `process-familia` **v24**. Adendo ADR-0004. Abreviações novas (RS/BCO/LRJ) ficaram de fora a pedido do Diego. |
| 2026-06-06 | **Selo de desconto "% OFF" — construído e ESTACIONADO (ADR-0017).** Superpowers completo (spec + plano + subagent-driven, 8 tasks de código): migration `configuracoes` (`desconto_pct` default 15) + `familias.exibir_com_desconto`/`desconto_pct`; pura `calcularPrecoDe`/`pctEfetivo` (TDD); `original_price` no CREATE/UPDATE; front (card do % em Config, checkbox+%+prévia por família sobre o `preco_publicacao`, toggle por lote). 274 testes. **Bug bash real matou a abordagem:** o `original_price` do `/items` foi **descontinuado** (item volta `original_price: null`); o selo exige `/seller-promotions` (`PRICE_DISCOUNT`) — **403 PolicyAgent** (app sem permissão de promoções) + AVILBV sem reputação verde/vendas (`level_id:null`, `transactions.completed:0`). Infra mantida **dormente**; workers `publish` v10/`update` v8 enviam `original_price` inofensivo (código morto a limpar). Endpoint/payload do PRICE_DISCOUNT já mapeados no ADR-0017 p/ quando a conta qualificar. Fixes de UX no caminho: prévia e cabeçalho da família passaram a usar o **preço de publicação** (não o da planilha). | 
| 2026-06-07 | **Correção da descrição (deploy defasado) + dimensões no frete.** (1) **Descrição saía com o preço da planilha** ("R$ 4,00 por unidade"): causa raiz NÃO era código — o `process-familia` em produção rodava uma versão **antiga** do `copywriter.ts` (M3, que injetava o preço no prompt); o repo já estava correto. A "dívida `_shared`" criou uma colcha de retalhos no deploy (mercado/redis/copywriter divergiam do repo). Fix: **redeploy limpo** via CLI (`process-familia` **v25**), realinhando produção ao repo. Validado: família reprocessada gera descrição sem preço. Junto: `tokens.ts` tinha `custo_centavos` 100×/1000× inflado (tratava $/1M como $/1k) → corrigido com TDD (commit `34d3756`). CLI do Supabase consertada (binário `supabase-go` + symlink); `SUPABASE_ACCESS_TOKEN` no `.env.local`. (2) **ADR-0018 — dimensões/peso no payload**: anúncio inativo por moderação de frete (`under_review`/`waiting_for_patch`, "não inclua serviço no frete") porque o app não enviava dimensões → ML estimava pacote grande. A planilha trazia placeholder fixo (0,1cm/100g nas 650 variações); Diego vai exportar dados reais. Pura `montarAtributosPacote`/`dimensoesValidas` (`_shared/ml/pacote.ts`, TDD) → atributos `SELLER_PACKAGE_*` (cm/g) no CREATE (`montarPayloadItem`) e UPDATE (adendo ADR-0016); guarda contra o placeholder; aviso "frete estimado" na Revisão (`PainelAnalise`). Formato `value_name "18 cm"` validado via `/items/validate` (ML aceitou). Workers `publish-familia-ml` **v11**/`update-familia-ml` **v11** deployados (verify_jwt false). 295 testes, tsc/lint verdes. **Pendente:** bug bash do frete com a planilha real (dimensões/peso). |
| 2026-06-06 | **Descrição reflete a cor nova no UPDATE** (adendo ADR-0016, TDD). Bug relatado pelo Diego: ao subir UPDATE com cor nova, a variação entrava no anúncio mas a seção **"🎨 CORES DISPONÍVEIS"** da descrição (texto da IA herdado do CREATE, sem IA no UPDATE por ADR-0016) continuava listando só as cores antigas. Fix: função pura `atualizarSecaoCores(descricao, cores)` (`_shared/ml/criar-item.ts`) reescreve **só a lista da seção de cores** (preserva o resto; sem cabeçalho → retorna intacto). `update-familia-ml` recalcula as cores incluídas (casadas+novas) em todo UPDATE de família já publicada e reenvia via `garantirDescricaoML` **só quando a lista muda** (guard `novaDescricao !== familia.descricao_ml` → idempotência no retry, sem reenvio redundante; `descricao_ml` no banco só atualiza após push ok). Falha no push sobe ao catch externo (5xx/429 → QStash reentrega; 4xx → `status=erro`). 6 testes em `descricao.test.ts` (inclui guard de retry). `update-familia-ml` **v10** deployada via MCP (produção). |
| 2026-06-07 | **Diagnóstico definitivo da descrição-com-preço + Parte A (limpeza) + Parte B (lacuna do UPDATE).** Causa raiz REAL (provada pelos tokens de input: prompt antigo=383, novo=1127): entre 06-01 e 06-05 um deploy arquivo-a-arquivo **reverteu acidentalmente o `copywriter.ts` do `process-familia` para o M3** (que injeta preço). Anúncio `MLB6914358210` foi publicado hoje 09:23Z ainda no copywriter velho; o redeploy v25 (correto) só entrou 10:07Z. Persistiu porque os lotes seguintes são **UPDATE** → ADR-0016 não roda IA → herdam a descrição publicada (velha-com-preço). **Parte A:** edge one-off descartável (reusou `getValidAccessToken`+`garantirDescricaoML` e a descrição limpa da família `d523d8b8`) deu push da descrição limpa em `MLB6914358210`; **verificado ao vivo no ML** (`live_tem_rs:false`); função apagada. **Parte B (adendo ADR-0016 2026-06-07, TDD):** o guard do `update-familia-ml` comparava a descrição contra o próprio banco (só pegava mudança de cor); agora compara a descrição **desejada (sanitizada)** contra a **ao vivo no ML** (`buscarDescricaoML` = GET grátis) via `resolverDescricaoUpdate` (puro) → reenvia quando difere (cor nova **ou** texto corrigido), não reenvia em reposição pura. **Custo de token: zero** (IA só no CREATE/botão Regenerar). Workers `update-familia-ml` **v12** + `publish-familia-ml` **v12** deployados via CLI (verify_jwt false), bundle verificado. 301 testes, tsc/lint verdes. |
| 2026-06-08 | **Excluir lotes + tela "Publicados" (ADR-0019)** — Superpowers completo (brainstorming → spec → plano, ambos com revisão de subagente; subagent-driven, 12 tasks). Branch `feat/excluir-lote-e-publicados` (aguardando review do Codex antes do merge). **F1 Excluir lote:** `particionarExclusao` (puro, TDD) preserva famílias publicadas (`ml_item_id`); edge `excluir-lote` (v1, verify_jwt true) com guarda de race (`processando`/`publicando`), limpeza de Storage (planilha+imagens menos as das publicadas) e **recontagem manual de contadores** (o trigger `update_lote_counters` não cobre DELETE); botão lixeira no `LoteCard` (AlertDialog). **F2 Publicados:** `parseStatusML`/`filtrarPublicados` (puros, TDD); edge `status-publicados` (v1) batch `GET /items?ids=` (envelope `{code,body}`, sem cache — volume baixo, YAGNI) + fallback sem credencial ML; edge `remover-publicado` (v1, guarda de `publicando` por `codigo_pai`, aviso cross-lote); tela `Publicados.tsx` (merge banco+status ao vivo, badges, filtros fornecedor/status/tipo/busca, "Abrir no ML", "Remover do sistema") + rota + item no menu. ML **nunca** tocado pela exclusão. Sem migration/schema novo. 316 testes verdes, tsc/lint/build limpos. Pendente: bug bash com token real + review do Codex. |
| 2026-06-08 | **Redesign PubliAI — Fases 1 a 3 ✅ (mergeado em `main`, commit `b7f4d87`).** Superpowers completo, faseado, com revisão por fase. **Fase 1 (Fundação/Design System):** paleta indigo/violeta em OKLCH, tokens semânticos `success/warning/danger/info` (light+dark), **dark como padrão** (`ThemeProvider` + anti-flash no `index.html`, persistência `localStorage['publiai-theme']`), escala tipográfica Geist (`text-display/h1/h2/h3/caption`), tokens de elevação/motion, primitivos shadcn (table/tabs/tooltip/sheet/avatar/skeleton/sonner/switch/separator/scroll-area) + recharts, componentes reutilizáveis (`PageHeader`, `Section`, `EmptyState`, `StatusPill`, `KpiCard`, `DataTable`), rota `/style-guide` e guia `docs/design-system/README.md`. **Fase 2 (App Shell):** `AppShell` responsivo (sidebar desktop + topbar com hamburguer mobile + drawer), `ThemeToggle`, `UserMenu`, Toaster. **Fase 3 (Revisão):** re-skin presentacional (zero mudança de lógica) de `Revisao`, `FamiliaRow`, `FamiliaExpanded`, `PainelAnalise`, `CardVoceRecebe` — mapa de tokens (cada cor hardcoded → `StatusPill`/token), filtros→`Tabs`, footer sticky, modal re-skin, ícones lucide no lugar de emojis. Specs/planos em `docs/superpowers/{specs,plans}/2026-06-08-redesign-fase{1,2,3}-*`. |
| 2026-06-09 | **Redesign PubliAI — Fase 4: telas restantes** (Superpowers completo: spec + plano em `docs/superpowers/{specs,plans}/2026-06-09-redesign-fase4-telas-restantes*`; subagent-driven, 6 tasks; branch `worktree-redesign-fase4`). Re-skin **100% presentacional, zero mudança de lógica** das telas que faltavam (as Fases 1–2 já davam shell+tokens globais; faltava o conteúdo). Aplicado o mesmo mapa de tokens da Fase 3: **Dashboard** (`PageHeader`+`EmptyState`), **`status-badge`** (`Badge`→`StatusPill` por tom, `LABELS` intactos), **RevisaoIndex** (`EmptyState`), **NovoLote** (`PageHeader`+`Progress` do DS), **Progresso** (`PageHeader`, banner de anomalias amber→token `warning`+`AlertTriangle`), **Relatório** (3 cards de status com tokens `success/info/destructive`+ícones lucide, link/erro via token), **Publicados** (tabela manual→primitivos `Table`, `BadgeStatus`→`StatusPill`, banners→tokens, `EmptyState`; `merged`/`filtrarPublicados`/`handleRemover`/dialogs intactos), **Configurações** (`PageHeader`, banners green/red→`success`/`destructive`, "Conectado"→`StatusPill`, `<input number>`→`Input` do DS), **auth** (Login/Cadastro/ResetSenha com tipografia DS) + **NotFound** (`EmptyState`/tokens). 338 testes verdes, tsc/lint (0 errors)/build limpos. **Mergeado em `main` (merge `42d1f4b`, push ok)** + fix da drop zone de imagens da Revisão (ilegível no dark → tokens + ícone lucide). |
| 2026-06-09 | **Redesign PubliAI — Fase 5: Dashboard com KPIs** (Superpowers completo: spec + plano em `docs/superpowers/{specs,plans}/2026-06-09-redesign-fase5-dashboard-kpis*`; execução inline TDD). Diferente das Fases 3–4 (re-skin), esta **adiciona funcionalidade**: o Dashboard (antes só lista de lotes) ganhou faixa de 5 `KpiCard` no topo. Função pura `calcularKpisDashboard(lotes, publicados, statusItens)` (`src/lib/dashboard-kpis.ts`, TDD 7 testes): **Anúncios publicados** (`usePublicados().length` — 1 por anúncio, não soma `totalPublicadas` p/ não duplicar em UPDATE), **Ativos** (status ao vivo = `ativo`), **Com problema** (`moderado`+`inativo`+`pausado`; exclui `encerrado`/`indisponivel`), **Erros de publicação** (Σ `totalErros`), **A revisar** (lotes em `revisao`). Os 3 cards de banco são instantâneos; os 2 ao vivo (Ativos/Com problema) usam `useStatusPublicados` com **skeleton no loading** e fallback **"—" + "ML indisponível"** quando sem credencial ou erro (nunca trava). Lista de lotes preservada abaixo; `PageHeader` "Lotes recentes"→"Dashboard". Sem schema/migration/edge nova (deriva de hooks existentes). YAGNI: sem filtro de período/delta/valor-em-estoque. Seletor do teste de rota ajustado (`/lotes recentes/`→`/dashboard/`). 345 testes verdes, tsc/lint/build limpos. |
| 2026-06-09 | **Redesign PubliAI — Fase 6: Acessibilidade (a11y)** (spec em `docs/superpowers/specs/2026-06-09-redesign-fase6-acessibilidade-design.md`; execução inline). Diagnóstico com `eslint-plugin-jsx-a11y` + varredura manual mostrou app já bem acessível (3 imagens com `alt`; 3/4 botões só-ícone com `aria-label`; reduced-motion nos tokens; foco/modais via Radix). **Correções:** (1) 5× `label-has-associated-control` — `htmlFor`+`id` nos labels do RadioGroup de estratégia (`Configuracoes`) e nos campos TÍTULO/DESCRIÇÃO (`familia-expanded`, `id` único por `familia.id`); (2) `aria-label` na lixeira do `LoteCard`. **Gate permanente:** `eslint-plugin-jsx-a11y` (recommended) adicionado ao `eslint.config.js` → `pnpm lint` agora checa a11y (0 errors). **Contraste WCAG AA:** auditoria por cálculo dos tokens OKLCH (OKLCH→sRGB→luminância, com composição correta do `bg-<tone>/10` sobre o card; sem axe pois o login bloqueia automação). Dark passava tudo; **light reprovava** os 4 StatusPills coloridos (warning 2.32:1, success 3.06, info 3.42, danger 4.09). Fix: escurecidos no `:root` os tons `success` 0.62→0.51, `warning` 0.72→0.53 (+`warning-foreground`→branco), `info` 0.60→0.52, `destructive`(=danger) 0.58→0.545 → todos os pills agora ≥4.6:1 em light e dark. Tons usados como fundo sólido só no StyleGuide (vitrine); nenhuma tela de produto afetada. Zero mudança de lógica. 345 testes verdes, tsc/lint/build limpos. YAGNI: teste de leitor de tela/teclado ponta-a-ponta fica manual. |
| 2026-06-09 | **Fix: metragem omitida no título (systematic-debugging + TDD).** Diego reportou que o título do anúncio perdia a metragem (ex.: `100MT`/`10MT`) presente na coluna NOME — pior: dois produtos distintos (10MT vs 100MT) recebiam **título idêntico**. Causa raiz: o dado **chega intacto** ao copywriter (`process-familia` passa `nome: nome_pai`), mas o **prompt do título não tinha slot para metragem** (formato `MARCA MODELO \| CARACTERÍSTICA \| DIFERENCIAL` + exemplo sem metragem); sob o teto de 60 chars a IA preenchia o "diferencial" com adjetivo genérico inventado. Fix **híbrido** (escolha do Diego): (1) **prompt** do `copywriter.ts` torna a metragem obrigatória no título (novo formato `MARCA MODELO METRAGEM \| …` + exemplo `FITA CETIM PROGRESSO N.1 100MT \| …`, priorizada sobre o diferencial); (2) **rede de segurança determinística** — função pura `garantirMetragemTitulo`/`extrairMetragem` (`_shared/ai/titulo.ts`, TDD 9 testes): regex extrai metragem do nome (MT/M/METROS; jardas não contam), injeta no título se a IA omitir, respeita 60 chars derrubando o diferencial antes de aparar, e não duplica se já presente. Fiada em `process-familia` e `regenerar-copy-familia`. Workers deployados via CLI: `process-familia` **v26** + `regenerar-copy-familia` **v4** (bundle verificado em produção). Backfill no banco das **3** famílias afetadas do lote atual (não publicadas, `ml_item_id=null`, `titulo_editado_pelo_operador=false`) — ML **não** tocado (estavam em Revisão). 358 testes verdes, lint 0 errors. Branch `worktree-fix-metragem-titulo`. |
| 2026-06-09 | **Fix: lote nunca transicionava `processando`→`revisao` (KPI "A revisar" sempre 0).** Diego reportou lotes em revisão que não apareciam no KPI do Dashboard (Fase 5, conta lotes em `revisao`). Causa raiz: a transição `processando→revisao` **nunca foi implementada** — o único set de `revisao` era pós-publicação (`publish/update-familia-ml`); `process-familia` marcava a família `pronto` mas nada flipava o lote, que ficava preso em `processando` para sempre. A tela de Revisão enganava porque lista **famílias** `pronto`, independente do status do lote. Fix na raiz: (1) **trigger `update_lote_counters`** (migration `20260609120000_lote_transicao_revisao`) ganhou um 2º UPDATE que flipa o lote `processando→revisao` quando não resta família `pendente`/`processando` — atômico contra a corrida do QStash (a última família a terminar é a única que satisfaz o `NOT EXISTS`; guard `status='processando'` torna idempotente e não toca `publicando`/`revisao`/`concluido`); validado transacionalmente (processando→processando→revisao). (2) **`ingest-lote` v19**: lote de reposição (UPDATE sem cor nova → famílias já `pronto`, nada enfileirado) vai direto a `revisao` em vez de `processando` (o trigger não cobre esse caso pois nenhuma família muda de status depois). (3) Backfill do lote #20 travado → `revisao`. Refina o lifecycle do ADR-0005. Sem mudança de frontend (o KPI já lia `lote.status` certo). 358 testes verdes. Branch `worktree-fix-lote-revisao`. |
| 2026-06-09 | **Estratégia de preço v2 (ADR-0020, substitui 0008).** PRECO da planilha passa a ser o líquido mínimo após comissão do ML. `process-familia`: com concorrente → `arredonda5(menor × 0,95)`; sem concorrente → gross-up `(piso+fixa)/(1−pct)` arredondado pra cima (comissão Clássico via `listing_prices`, 1× por família). Arredondamento sempre em múltiplos de R$ 0,05 (`_shared/preco/arredondar.ts`, TDD). `_shared/preco/sugerir.ts` substitui `calcular.ts`; corrigido bug do enum `estrategia_preco` (comparava `'COMPETITIVO'` maiúsculo → sempre gravava `proprio`). `buscarListingPrice`/`comissaoDe` extraídos p/ `_shared/ml/listing-prices.ts` (reuso com `calcular-tarifa-ml`). **Semáforo** "vale a pena publicar?" no front (`src/lib/semaforo.ts` + `SemaforoPreco`): 🟢 líquido ≥ PRECO, 🟡 custo ≤ líquido < PRECO, 🔴 líquido < custo; família = pior caso (variação de menor preço); badge 🚚 acima de R$ 19. Sem migration. Spec/plano em `docs/superpowers/{specs,plans}/2026-06-09-estrategia-preco-v2*`. **Pendente:** deploy de `process-familia`/`calcular-tarifa-ml` via CLI + bug bash com token real. |
| 2026-06-09 | **Fix: a 2ª foto comum (`CAPA2_`) virava capa principal no ML (systematic-debugging + TDD).** Diego subiu um lote cuja família 00445916 não tinha foto-capa própria (só `CAPA2_` + fotos das cores, sem arquivo `CAPA_` nem foto do PAI). Causa raiz: `montarPayloadItem`/`montarVariacaoNova` montavam as fotos como `[capa, capa2, fotoDaCor]` filtrando nulos; com `capa_storage_path` nula, a `capa2` escorregava para a 1ª `picture_id` — que o ML usa como capa da galeria de cada variação. A Revisão *parecia* certa porque o preview do front tem um fallback (`capaStoragePath ?? fotoCapaPath` = foto da 1ª variação) que **nunca era persistido** em `capa_storage_path` nem usado pelo worker. Fix (adendo ADR-0003, TDD): helper puro `ordenarFotosVariacao(capa, capa2, propria)` em `_shared/ml/publicar.ts` — a 1ª posição é sempre uma foto principal (capa da família, ou a própria foto da cor sem capa); a capa2 **nunca** lidera. Aplicado no CREATE (`montarPayloadItem`, per-variação + `item.pictures`) e na cor nova do UPDATE (`montarVariacaoNova`); o caminho de variações existentes do UPDATE já estava correto. Workers `publish-familia-ml` **v14** + `update-familia-ml` **v14** deployados via CLI (bundle verificado). Único anúncio afetado (`MLB4752971229`, 43 cores) reordenado ao vivo no ML via edge one-off descartável (`getValidAccessToken` + `ordenarFotosVariacao` → PUT `picture_ids=[própria, capa2]`), confirmado e função apagada. 399 testes verdes, tsc/lint limpos. Branch `worktree-fix-capa2-ordem-fotos`. |
| 2026-06-09 | **Seção "CORES DISPONÍVEIS" da descrição sempre em ordem alfabética (TDD).** A lista de cores saía na ordem das variações (por código), pedido do Diego para ficar alfabética. Helper puro `ordenarCoresAlfabetica` (`_shared/cor/ordenar.ts`, `localeCompare` pt-BR com `sensitivity:'base'` + `numeric:true` → case/acento-insensível e sufixo numérico natural; não muta a entrada). Aplicado em **dois pontos**: (1) input da IA no `copywriter.ts` (`coresUnicas` ordenado antes do prompt → CREATE alfabético) e (2) `atualizarSecaoCores` (`criar-item.ts`) passou a ordenar a lista que escreve → todo caminho determinístico (UPDATE de cor nova, `resolverDescricaoUpdate`, reescrita) fica alfabético. Workers redeployados via CLI: `process-familia`, `regenerar-copy-familia` (copywriter) + `publish-familia-ml`, `update-familia-ml` (criar-item). **Anúncios já publicados atualizados ao vivo** via edge one-off descartável: extrai os nomes já presentes na seção (preserva os nomes exatos), reordena via `atualizarSecaoCores`, persiste em `familias.descricao_ml` e dá push via `garantirDescricaoML` — 5 de 6 reordenados (1 já alfabético), zero erros, confirmado no banco/ML; função apagada. TDD: `cor/ordenar` (5) + `atualizarSecaoCores` alfabético (1); testes de ordem existentes atualizados. 405 testes verdes, tsc/lint limpos. Branch `worktree-cores-ordem-alfabetica`. |
| 2026-06-09 | **Fix: contagem/filtro "Incompletas" na Revisão contava famílias já publicadas (TDD, frontend).** Diego publicou tudo e a aba "Incompletas" continuava em 3. Causa raiz: `familiaPublicavel` só aceita status `'pronto'`/`'erro'` (linha 37) → família `'publicado'` cai em "Ainda em processamento" → `ok:false`; a aba/contagem (`Revisao.tsx`) usava `!familiaPublicavel(f).ok`, sem o guard de publicado que a **linha** já tinha (`familia-row` só mostra o selo quando `!publicado`) — daí a aba divergia das linhas. Fix: helper `familiaIncompleta(familia)` em `publicavel.ts` (publicado/publicando → nunca incompleta; senão `!familiaPublicavel.ok`) usado como **fonte única** no filtro, na contagem da aba e no selo da linha (elimina a divergência). Conceito separado de "publicável" (já publicada não é mais publicável, mas também não é incompleta). Sem backend/ML/migration — só frontend (atualiza no Render via push). TDD: 5 testes de `familiaIncompleta`. 410 testes verdes, tsc/lint limpos. Branch `worktree-fix-contagem-incompletas`. |
| 2026-06-09 | **Fix: "É dupla face: Sim" indevido nas fitas (systematic-debugging + TDD).** Diego reportou que os anúncios de fita exibiam "É dupla face: **Sim**" sendo todos face simples. Causa raiz: o `montarAtributosML` **nunca enviava** o atributo `IS_DOUBLE_FACE` (categoria `MLB255054`); omitido, o **ML auto-preenche "Sim"** (as 4 fitas publicadas tinham o atributo nulo no banco — confirmado por SQL). Fix determinístico (adendo ADR-0009, TDD): fita passa a **sempre** emitir `IS_DOUBLE_FACE` — `value_id 242084` (**Não**, face simples) por padrão e `242085` (**Sim**) só quando a `DESCRICAO_DETALHADO` da planilha indicar (`ehDuplaFace`: regex `dupla face|face dupla|duas faces|dois lados`, case/acento-insensível). `montarAtributosML` ganhou 4º param `detalhe`; `process-familia` passa `descricao_pai`. Linha/botão não recebem o atributo (só fita). Valores validados via `GET /categories/MLB255054/attributes`. Workers `process-familia` + `publish-familia-ml` redeployados via CLI (bundle verificado contém `IS_DOUBLE_FACE`/`ehDuplaFace`). **4 fitas já publicadas corrigidas ao vivo** via edge one-off descartável (PUT `IS_DOUBLE_FACE=242084` por item + alinha `atributos_ml` no banco; confirmado `live_value_name:"Não"` nas 4, banco alinhado; função apagada). 410 testes verdes, tsc/lint limpos. Branch `worktree-fix-dupla-face-atributo`. |
| 2026-06-10 | **3ª foto comum por família (`CAPA3_`)** (Superpowers completo: brainstorming → spec `2026-06-10-terceira-foto-comum-capa3-design.md` → execução TDD inline). O ML recomenda 3 fotos/anúncio; o app enviava 2 comuns (capa + `CAPA2_`). **Investigação:** o "Gerar fotos com IA" do editor web do ML é **UI-only** — não exposto pela API pública (só `POST /pictures`, diagnóstico e substituição; a IA de imagem é o projeto interno GenAds, sem API p/ terceiros). Então a 3ª foto é uma **3ª foto comum**, sem IA (evita inventar a aparência do produto real + risco de moderação). Espelho fiel da infra `CAPA2_`. **Ordem (decisão Diego):** a `CAPA3` entra logo após a `CAPA2`, padrão p/ toda a família — `[líder, capa2, capa3, foto-da-cor]`, onde líder = capa-da-família ?? foto-da-cor; capa2/capa3 nunca lideram. Migration aditiva `familias.capa3_storage_path`/`capa3_ml_picture_id`. `classificarArquivo`/`matchCapa3` (prefixo `CAPA3_00CODIGO`, upload em `capas3/`); `ingest-lote` seta capa3 no CREATE+UPDATE; `ordenarFotosVariacao`/`montarPayloadItem`/`montarVariacaoNova` ganham o param `capa3` (após `capa2`); `publish`/`update` workers resolvem+sobem+propagam a capa3 (UPDATE generaliza capa2/capa3 como `comuns`, idempotente); `exclusao`/`remover-publicado` preservam/limpam capa3 do Storage; front: tipo+adapter+`subirCapa3Familia`/`removerCapa3Familia` + 3º bloco de foto na Revisão. Adendo ADR-0003/ADR-0016. TDD: `match-capa3` + `publicar`/`atualizar`/`exclusao` estendidos (250 `_shared` verdes; tsc/lint limpos). **Deploy via CLI (verify_jwt preservado): `publish-familia-ml`/`update-familia-ml` (no-verify-jwt) + `ingest-lote`/`upload-imagens-lote`/`excluir-lote`/`remover-publicado`; bundle `update-familia-ml` v16 verificado com a lógica capa3.** Branch `worktree-feat-capa3-terceira-foto`. **Pendente:** push do frontend p/ Render (botão "Subir 3ª foto") + bug bash com token real (CAPA3_ em lote e botão; conferir 3ª foto na galeria do ML em CREATE e UPDATE). |
| 2026-06-10 | **Vinculação automática ao Catálogo do ML (ADR-0021)** — Superpowers completo (brainstorming → spec `2026-06-10-catalogo-optin-design.md` → TDD inline). Resolve a ameaça de pausa por catálogo ("Verifique o produto de catálogo... antes de [data]" / "Busque sua variação para competir" = tag `catalog_forewarning` → moderação `OPT_OBEY`). **Validação com token real (edge `probe-catalogo` descartável, removida):** aviamentos são `catalog_required` mas **não** `catalog_only` (só 11 domínios, eletrônicos/medicamentos) → o opt-in **não inativa** o anúncio de marketplace; `/products/search?product_identifier={gtin}` casa o produto de catálogo **exato** por GTIN real; `/items/{id}/catalog_listing_eligibility` decide por variação — anúncio de cores da **mesma família** vem `READY_FOR_OPTIN`/`buy_box_eligible`, anúncio multi-cor de **famílias diferentes** vem `FAMILY_DIFF` (bloqueado). O item de linha do print (`MLB6901096672`) está `READY_FOR_OPTIN` com `moderation_date 2026-06-16`. **Decisões (brainstorming):** estratégia híbrida (opt-in só por GTIN real, nunca por texto), só automação futura (CREATE/UPDATE; já-publicados o operador resgata manual), opt-in automático no publish travado por elegibilidade. **Implementação:** `_shared/ml/catalogo.ts` (puras `decidirAcaoCatalogo`/`montarBodyOptin`/`indexarEligibility` + rede `buscarProdutoCatalogoPorGtin`/`buscarElegibilidadeCatalogo`/`optinCatalogo` + orquestrador `vincularVariacoesCatalogo`, best-effort/idempotente via `catalog_listing_id`); `publish-familia-ml` (CREATE) e `update-familia-ml` (UPDATE) chamam o passo no fim, após casar `ml_variation_id`. Migration aditiva em `variacoes` (`catalog_product_id`/`catalog_listing_id`/`catalog_status`/`catalog_erro`) aplicada. TDD: `catalogo.test.ts` (10). Workers redeployados via CLI (bundle com `catalogo.ts`). Build/lint verdes. Branch `worktree-catalogo-optin`. **Verificação ao vivo (OK do Diego):** opt-in real do item de linha do print (`MLB6901096672`) via edge one-off descartável que chama o próprio `vincularVariacoesCatalogo` (código deployado) — 2 cores `vinculado`, 0 erros; criou 2 anúncios de catálogo (`MLB6937529524` Branco / `MLB4758991257` Preto, `catalog_listing:true`, ativos), cada um com `item_relations:[{id:MLB6901096672, variation_id, stock_relation:1}]` (estoque espelhado, sem duplicar) e `GET /public/buybox/sync` = **SYNC**; banco persistiu `catalog_listing_id`/`catalog_status='vinculado'`. A tag `catalog_forewarning` no original cai no batch assíncrono do ML (o vínculo já existe → `OPT_OBEY`, que só pausa itens sem catálogo, não se aplica). Edge one-off apagada. Mergeado em `main` (merge `436d93c`, push ok). |
| 2026-06-10 | **Bug bash do catálogo (lote 25) → fix: opt-in deferido via QStash (ADR-0021 revisto).** Diego subiu o lote 25 (2 fitas: N.12 com 2 cores, N.2 com 77) e o opt-in marcou **79/79 `nao_elegivel`**, sem vincular nada. **Causa raiz:** o `/items/{id}/catalog_listing_eligibility` **não está pronto no instante do `POST /items`** — leva minutos e passa por estados **transitórios** (o item de 77 cores apareceu `READY_FOR_OPTIN` em 100% por instantes e depois assentou em `FAMILY_DIFF`; o de 2 cores ficou `READY` por ser mesma família). O passo síncrono no publish via tudo vazio → `nao_elegivel`. **Fix:** (1) o opt-in deixa de rodar síncrono — `publish`/`update` **enfileiram** um job `vincular-catalogo` (novo worker QStash, verify_jwt false) com **delay 600s (deixa a elegibilidade assentar) + retries 5** (`_shared/queue.ts` → `enfileirarVinculacaoCatalogo`); o worker devolve 500 enquanto houver variação `pendente` (QStash retenta). (2) `decidirAcaoCatalogo` distingue **`pendente`** (elegibilidade ausente/sem status = ainda computando, retentável) de `nao_elegivel` (status explícito do ML); `ResumoCatalogo` ganha `pendente`. **Re-rodado o lote 25 ao vivo (orquestrador real, one-off descartável):** N.12 → **2 `vinculado`** (`MLB4759225507`/`MLB4759225495`, `catalog_listing:true`, ativos, `stock_relation`=1, buybox **SYNC**); N.2 → **77 `family_diff`** (corretamente não forçado — multi-família). 267 testes `_shared` verdes (catalogo.test 11), lint 0 errors. Workers `vincular-catalogo`+`publish-familia-ml`+`update-familia-ml` deployados via CLI; probes descartáveis removidos. |
| 2026-06-11 | **Lote #26 (cola em bastão): 3 bugs corrigidos na raiz (systematic-debugging + TDD, ADR-0022).** (1) **Título cortado** ("…IDEAL PARA P"): o schema do `copywriter` tinha `maxLength:60` que **cortava a string mecanicamente no meio da palavra**; removido o `maxLength` (a IA devolve o título inteiro) + `clampTitulo` (`_shared/ai/titulo.ts`) que enforça 60 chars derrubando segmentos/palavras **inteiras**, nunca mid-word. (2) **CAPA2/CAPA3 sumiam na Revisão**: o operador nomeou as fotos comuns com o código vendável (filho `02841037`/`02841053`), mas `matchCapa/2/3` só procurava pelo código do PAI; agora aceitam PAI **ou** qualquer código de variação da família (`ingest-lote` passa `[pai, ...variações]`). (3) **Categoria indefinida**: "cola" caía em `outro`. Novo tipo determinístico **`cola` → `MLB277319`** "Bastões de Cola" (folha, `BRAND`+`MODEL`, expõe `EMPTY_GTIN_REASON` value `17055160` — tudo validado na API ML), regex `cola`/`colas`, migration aditiva no enum `tipo_aviamento`. Além disso, **seletor manual de categoria** na Revisão (`Select` no `CardCategoria` → edge nova **`definir-categoria-familia`** reusando `_shared`, sem duplicar lógica) como escape hatch p/ qualquer produto `outro`. **Backfill do lote #26** (não publicado): títulos limpos, capa2/3 apontadas, categoria cola + atributos. 465 testes verdes, tsc/lint/build limpos. Deploy via CLI (verify_jwt preservado): `ingest-lote` v22, `process-familia` v31, `regenerar-copy-familia` v7, `publish-familia-ml` v21, `update-familia-ml` v20, `definir-categoria-familia` v1 (nova); bundle conferido. `main` push + Render. **Pendência (bug bash):** a cola **não tem cor** e hoje entra como variação única com COLOR="Outra"/"Branco" — validar se o ML aceita na publicação real. Branch `worktree-fix-lote26-cola`. |
