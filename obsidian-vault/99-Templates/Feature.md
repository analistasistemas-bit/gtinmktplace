---
tags: [template, feature]
---

# Nome da feature/módulo

**Status:** Não implementado | Em desenvolvimento | Em produção
**Épico/ADR relacionado:**

## O que é

Uma frase direta do que a feature faz e para quem.

## Onde vive no código

- Telas (`src/pages/`):
- Componentes (`src/components/`):
- Hooks (`src/hooks/`):
- Edge functions (`supabase/functions/`):
- Tabelas (`supabase/migrations/`):

## Como funciona

Fluxo principal, com diagrama Mermaid se ajudar.

## Dependências

O que precisa existir antes desta feature funcionar.

## Status real (não o desejado)

O que já funciona vs. o que está pendente — seja explícito se algo ainda não foi implementado.

---

Formato inspirado nas notas de [[Produtos]], [[Marketplace]] e [[Publicação Shopee]] (esta
última é o exemplo de como documentar uma feature **planejada mas não implementada** sem
inventar detalhes de implementação que não existem).
